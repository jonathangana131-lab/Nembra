import NembraCore

// Keep the package's capture-domain names explicitly bound to the canonical
// NembraCore model. This is a compile-time bridge only; it adds no runtime,
// Bluetooth, evidence, or authorization behavior.
public typealias PassiveBluetoothCaptureSession = NembraCore.PassiveBluetoothCaptureSession
public typealias PassiveBluetoothCaptureJSON = NembraCore.PassiveBluetoothCaptureJSON
