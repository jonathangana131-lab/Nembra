# CAPTURE HARD FREEZE — FASTEST PATH TO FIRST ES80 ARTIFACT

This file is the active execution lock for the one-time Nembra Capture utility. Live GitHub remains authoritative; if any value below differs from the current PR/run state, refresh GitHub before acting.

## Frozen candidate
- PR: #833
- exact product head: `f4cd76e301334ce96824d0b150ef03d2d2cb606b`
- exact-head portable/source Capture guards: **7 / 7 SUCCESS**
- current PR-local Xcode run: `31294595269`
- resolver job: `93197494705` — **SUCCESS**
- field-authority tooling job: `93197500421` — **SUCCESS**
- Mac job: `93197500422` — **QUEUED / NON-EVIDENCE** at this checkpoint
- all Xcode results on ancestor heads are supporting history only and do not accept `f4cd76e...`

The exact Capture product head is frozen while this exact-head runtime acceptance is alive. Do not issue another `/xcode27` or `/capture-xcode27` merely because the Mac job is queued.

## Why the prior exact head was rejected

Exact ancestor `8efdc2946f784c2bf2130d4c3447baeb15e895a6` did **not** earn runtime acceptance. Run `31292505064`, Mac job `93191996583`, passed the package/truth/provenance gates and then failed the real Simulator matrix on four Accessibility XXXL geometry assertions:
- locked NO-GO build identity clipped below the iPhone 12 viewport;
- Capture Complete synthetic QA disclosure clipped below the viewport;
- Horizon Ready synthetic QA disclosure clipped below the viewport;
- Horizon Ready Capture progress overshot above the viewport after the old helper kept moving in one direction.

Those failures were treated as product/acceptance evidence, not ignored.

Current `f4cd76e...` contains the bounded recovery:
- true Accessibility-size recomposition for the affected NO-GO/Capture shell states while preserving the user's actual XXXL setting and semantic truth;
- no global Dynamic Type cap or test-only product lie;
- a one-file UI-test harness repair so viewport navigation reverses direction when a target overshoots above vs remains below the viewport;
- the strict final full-window visibility assertion remains unchanged.

The final `f4cd76e...` helper repair changes no app/package/BLE/provenance/signing/ResearchAdmission/physical procedure bytes.

## Prime rule

**DO NOT MOVE #833 WHILE CURRENT EXACT-HEAD XCODE RUN `31294595269` / MAC JOB `93197500422` IS QUEUED OR IN PROGRESS, unless newly demonstrated exact-current TODAY evidence requires a bounded repair.**

Do not mutate the Capture flagship for speculative hardening, cosmetics, documentation, duplicate validation, test cleanup, branch hygiene, post-capture security work, or a theoretically stronger implementation while this exact run is alive.

Move the frozen candidate only for a demonstrated normal-path TODAY blocker under `CAPTURE_TODAY_FIELD_READY_DIRECTIVE.md`: build/install/launch failure; Capture crash/hang/corruption/mis-target/export failure; application characteristic-write authority; Stationary/Charger Disconnected bypass; exact-head acceptance false-green/failure; real Accessibility XXXL runtime failure; signed intended-device installation failure; missing package-owned `PRIVATE RESEARCH BUILD / Runtime provenance ready` on the exact retained IPA; or inability to deliberately authorize the exact safe Research Field Build.

## Exact retained-evidence acceptance target

A green workflow alone is insufficient. If the current Mac job succeeds, inspect exact-`f4cd76e...` retained evidence before claiming acceptance, including:
- locked NO-GO Accessibility XXXL;
- Capture Complete Accessibility XXXL;
- Horizon Ready Accessibility XXXL progress / health / seal;
- `Capture Stationary Preflight to OFF 1 Synthetic` handoff into the real Capture shell;
- representative ordinary routing states retained by the matrix;
- xcresult and logs;
- `NembraCaptureExternalBuildRecord.json`;
- executable SHA-256 and raw Info.plist SHA-256;
- exact source/build/build-instance/recipe tuple.

The sole unresolved #833 review thread for the preflight -> real Capture shell handoff remains open until exact-current retained runtime/provenance evidence proves it.

## What happens when the exact-head run finishes

### If terminal SUCCESS
1. inspect the exact retained runtime/visual/provenance matrix above;
2. if no TODAY blocker remains, earn exactly one trusted default-branch `/capture-xcode27` gate for the same unchanged SHA (unless equivalent trusted exact-head evidence already exists);
3. prove the canonical TODAY Research flag reaches package-owned `NembraBluetoothCapture` on the actual physical-iOS Release **archive** action before relying on ResearchAdmission;
4. produce one exact signed `ES80-FINGERPRINT-v1` private Research Field Build via `scripts/ci/xcode27_today_research_field_candidate.sh` on the private signing surface;
5. independently inspect signing/provisioning/intended-device membership and exact source/build/build-instance/recipe/executable/Info.plist/evidence-record/IPA hashes;
6. run the pinned external retained-candidate cross-check and require the accepted Research compile tuple while preserving `PASS_NOT_FINAL_GO` / physical authorization `not-granted`;
7. install exactly that retained IPA without rebuilding/re-exporting using `docs/ES80_TODAY_EXACT_RETAINED_IPA_INSTALL.md`, launch from the Home Screen, and require exact runtime provenance plus package-owned ResearchAdmission;
8. complete the external TODAY Final GO instrument only after its exact authority and durable-publication gates are accepted;
9. only then run the one-time stationary, charger-disconnected, passive/read-only ES80 capture, observe at least 60 seconds after accepted Ready, seal Horizon, and preserve the exact raw Share artifact unchanged.

### If terminal FAILURE
Only the demonstrated failing step becomes Capture work. Repair that exact failure with the minimum coherent change, freeze the new exact SHA, and run one new exact-head acceptance. Do not reopen unrelated work.

### If cancelled or skipped
Determine why before issuing another command. A stale-head cancellation after a legitimate #833 move is expected non-evidence. Cancelled/skipped on an unchanged exact current head is not green.

## Parallel TODAY field-unblocking state

Keep external tooling work off the frozen app head unless it exposes a real app blocker.

- The exact retained-IPA install/runbook path on current `main` is aligned with verifier commit `d827a296048386bda62024ea3278775d5344c47c`, verifier blob `c3b2b620280484c05316fc5c2fa2ca451f1fdc83`, and Research tuple `private-today-v1 / canonical-producer-explicit-mode / NEMBRA_ES80_TODAY_RESEARCH`.
- Final-GO convergence #1532 is external tooling only. Its Research tuple recovery is accepted in source, but a failed post-rename publication must not leave a durable `FinalGO.json` that still says `GO`; exact expected-red validation has demonstrated that blocker until the canonical helper closes rollback/quarantine semantics.
- Archive-propagation validation #1535 is the current exact-`f4cd76e...` workflow-only probe for whether the producer's command-line Research condition reaches package-owned `NembraBluetoothCapture` on the real archive action. Keep it draft while #833 owns the scarce Mac lane.

## Physical boundary

This hard freeze does not authorize hardware. Physical Experiment One remains **NO-GO / DO NOT RUN** until exact-head trusted acceptance, retained artifact/screenshot inspection, Research archive propagation proof, exact signed retained IPA inspection, intended-device installation/runtime rendezvous, fresh Stationary + Charger Disconnected preflight, and the accepted external Final GO record are all complete.
