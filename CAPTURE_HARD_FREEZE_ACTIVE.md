# CAPTURE HARD FREEZE — FASTEST PATH TO FIRST ES80 ARTIFACT

This file is an active execution lock for the one-time Nembra Capture utility.

## Frozen candidate
- PR: #833
- exact product head: `8fe891fb66d3e78a0f455ff6d20343513af1f5e1`
- trusted exact-head Xcode run: `31289970813`
- Mac job: `93185368211`

At activation, the Mac job is actively running on the exact frozen SHA. Project structure and NembraCore have already passed and the job is executing the Capture package.

## Prime rule

**DO NOT MOVE #833 WHILE THIS EXACT MAC RUN IS QUEUED OR IN PROGRESS.**

No worker may mutate the Capture flagship for speculative hardening, acceptance-checker improvements, cosmetics, documentation, duplicate validation, test cleanup, or a theoretically stronger implementation while this exact run is alive.

A finding discovered during the live run is recorded for later; it does not move the frozen SHA unless it proves an immediate safety-critical defect that makes continuing the current run or eventual private stationary passive experiment unsafe.

## What happens when the run finishes

### If terminal SUCCESS
Do not reopen software polishing. Immediately advance the same exact accepted source to:
1. inspect retained Xcode/runtime/screenshots/evidence;
2. produce exact signed `ES80-FINGERPRINT-v1` private Research Field Build;
3. inspect retained IPA signing/provisioning/intended-device membership and exact source/build/build-instance/recipe/hashes/IPA digest;
4. install that exact retained IPA without rebuilding;
5. verify `PRIVATE RESEARCH BUILD / Runtime provenance ready` and the visible recipe/build/source/build-instance tuple before scan;
6. complete the external TODAY Final GO record;
7. run the one-time stationary, charger-disconnected, passive/read-only ES80 capture and preserve the raw Share artifact unchanged.

### If terminal FAILURE
Only the demonstrated failing step becomes Capture work. Assign the minimum crew needed to repair that exact failure, compose one bounded fix, freeze the new exact SHA, and run one new exact-head acceptance. All unrelated findings remain deferred.

## Everyone else
All workers not required for the exact Capture reaction crew must work on the actual Nembra product under `ADAPTIVE_SWARM_PRIORITY.md`.

Current overflow priority:
1. Dashboard / Cockpit
2. Battery + Range
3. Rides + Records
4. Navigation
5. Home
6. Vehicle / Controls

Do not poll the Mac job. Do not open duplicate exact-head validation PRs. Do not create new Capture work simply because Capture is P0.

## Capture product scope
Capture is a one-time evidence utility, not a second flagship product. It needs to be safe, simple, clean, understandable, and capable of producing one trustworthy Bluetooth artifact. Premium visual/product effort belongs in the real Nembra app.

## Physical boundary
This hard freeze does not itself authorize hardware. Physical Experiment One remains NO-GO until exact-head acceptance, signed retained IPA inspection, intended-device installation/runtime rendezvous, and the external Final GO record are complete.
