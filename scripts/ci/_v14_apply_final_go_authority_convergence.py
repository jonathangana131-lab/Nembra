#!/usr/bin/env python3
from pathlib import Path

ROOT = Path(__file__).resolve().parents[2]
SOURCE = ROOT / "scripts/ci/es80_today_final_go_record.py"
TESTS = ROOT / "scripts/ci/tests/test_es80_today_final_go_record.py"
WORKFLOW = ROOT / ".github/workflows/capture-today-final-go-qa.yml"


def replace_once(text: str, old: str, new: str, label: str) -> str:
    count = text.count(old)
    if count != 1:
        raise SystemExit(f"{label}: expected one match, found {count}")
    return text.replace(old, new, 1)


def replace_span(text: str, start_marker: str, end_marker: str, replacement: str, label: str) -> str:
    start = text.find(start_marker)
    if start < 0:
        raise SystemExit(f"{label}: missing start marker")
    end = text.find(end_marker, start)
    if end < 0:
        raise SystemExit(f"{label}: missing end marker")
    return text[:start] + replacement + text[end:]


source = SOURCE.read_text(encoding="utf-8")
source = replace_once(
    source,
    '''REPOSITORY = "jonathangana131-lab/Nembra"\nTRUSTED_WORKFLOW_NAME = "Xcode 27 PR Exact-Head QA"\nTRUSTED_WORKFLOW_PATH = ".github/workflows/xcode27-pr-command.yml"\nTRUSTED_JOB_NAME = "Build, test, and capture exact PR head"\nTRUSTED_ARTIFACT_PREFIX = "nembra-xcode27-pr-"\n''',
    '''REPOSITORY = "jonathangana131-lab/Nembra"\nREPOSITORY_OWNER = "jonathangana131-lab"\nDEFAULT_BRANCH = "main"\nTRUSTED_WORKFLOW_NAME = "Capture Trusted Xcode 27 Exact-Head QA"\nTRUSTED_WORKFLOW_PATH = ".github/workflows/capture-xcode27-trusted-command.yml"\nTRUSTED_WORKFLOW_BLOB_SHA = "e5ef72f50bed279e98ad28b94930831b747d0c20"\nTRUSTED_JOB_NAME = "Build, test, and capture trusted exact Capture head"\nTRUSTED_ARTIFACT_PREFIX = "nembra-capture-xcode27-"\n''',
    "trusted constants",
)

trusted_function = '''def _trusted_xcode_subject(\n    *,\n    source: str,\n    expected_pr_number: int,\n    run_id: int,\n    job_id: int,\n    artifact_id: int,\n    artifact_archive_path: Path,\n    tooling_repo: Path,\n    github_get_json: Callable[[str], tuple[bytes, dict[str, Any]]],\n) -> dict[str, Any]:\n    pr_raw, pr = github_get_json(f"/pulls/{expected_pr_number}")\n    _eq(_positive_int(pr.get("number"), "trusted Capture PR number"), expected_pr_number, "trusted Capture PR number")\n    _eq(pr.get("state"), "open", "trusted Capture PR state")\n    head = pr.get("head")\n    base = pr.get("base")\n    if not isinstance(head, dict) or not isinstance(base, dict):\n        raise FinalGoError("trusted Capture PR lacks head/base subjects")\n    _eq(_shape(head.get("sha"), HEX40, "live Capture PR head SHA"), source, "live Capture PR head SHA")\n    if not isinstance(head.get("repo"), dict) or head["repo"].get("full_name") != REPOSITORY:\n        raise FinalGoError("trusted Capture PR head repository is not canonical Nembra")\n    if not isinstance(base.get("repo"), dict) or base["repo"].get("full_name") != REPOSITORY:\n        raise FinalGoError("trusted Capture PR base repository is not canonical Nembra")\n\n    run_raw, run = github_get_json(f"/actions/runs/{run_id}")\n    _eq(_positive_int(run.get("id"), "trusted Xcode run ID"), run_id, "trusted Xcode run ID")\n    _eq(run.get("name"), TRUSTED_WORKFLOW_NAME, "trusted Xcode workflow name")\n    _eq(run.get("path"), TRUSTED_WORKFLOW_PATH, "trusted Xcode workflow path")\n    _eq(run.get("event"), "issue_comment", "trusted Xcode workflow event")\n    _eq(run.get("head_branch"), DEFAULT_BRANCH, "trusted Xcode workflow branch")\n    _eq(run.get("status"), "completed", "trusted Xcode run status")\n    _eq(run.get("conclusion"), "success", "trusted Xcode run conclusion")\n    repository = run.get("repository")\n    head_repository = run.get("head_repository")\n    if not isinstance(repository, dict) or repository.get("full_name") != REPOSITORY:\n        raise FinalGoError("trusted Xcode run repository is not canonical Nembra")\n    if not isinstance(head_repository, dict) or head_repository.get("full_name") != REPOSITORY:\n        raise FinalGoError("trusted Xcode run head repository is not canonical Nembra")\n    actor = run.get("actor")\n    triggering_actor = run.get("triggering_actor")\n    if not isinstance(actor, dict) or actor.get("login") != REPOSITORY_OWNER:\n        raise FinalGoError("trusted Xcode command actor is not repository owner")\n    if not isinstance(triggering_actor, dict) or triggering_actor.get("login") != REPOSITORY_OWNER:\n        raise FinalGoError("trusted Xcode triggering actor is not repository owner")\n\n    workflow_source = _shape(run.get("head_sha"), HEX40, "trusted Xcode workflow source SHA")\n    _eq(\n        _git(tooling_repo, "rev-parse", "--verify", f"{workflow_source}^{{commit}}"),\n        workflow_source,\n        "trusted Xcode workflow source commit",\n    )\n    workflow_blob = _shape(\n        _git(tooling_repo, "rev-parse", f"{workflow_source}:{TRUSTED_WORKFLOW_PATH}"),\n        GIT_OID,\n        "trusted Xcode workflow Git blob",\n    )\n    _eq(workflow_blob, TRUSTED_WORKFLOW_BLOB_SHA, "trusted Xcode workflow Git blob")\n\n    run_attempt = _positive_int(run.get("run_attempt"), "trusted Xcode run attempt")\n    run_number = _positive_int(run.get("run_number"), "trusted Xcode run number")\n\n    job_raw, job = github_get_json(f"/actions/jobs/{job_id}")\n    _eq(_positive_int(job.get("id"), "trusted Xcode job ID"), job_id, "trusted Xcode job ID")\n    _eq(job.get("run_id"), run_id, "trusted Xcode job run ID")\n    _eq(job.get("run_attempt"), run_attempt, "trusted Xcode job run attempt")\n    _eq(job.get("workflow_name"), TRUSTED_WORKFLOW_NAME, "trusted Xcode job workflow name")\n    _eq(job.get("name"), TRUSTED_JOB_NAME, "trusted Xcode job name")\n    _eq(job.get("head_sha"), workflow_source, "trusted Xcode job workflow source SHA")\n    _eq(job.get("status"), "completed", "trusted Xcode job status")\n    _eq(job.get("conclusion"), "success", "trusted Xcode job conclusion")\n    labels = job.get("labels")\n    if not isinstance(labels, list) or "xcode-27" not in labels:\n        raise FinalGoError("trusted Xcode job did not run on the xcode-27 runner class")\n    required_steps = {\n        "Reject stale or detached Capture head before scarce Mac work",\n        "Verify immutable trusted Capture head",\n        "Build, test, and capture Simulator states",\n        "Verify retained Capture evidence against trusted resolver authority",\n        "Reject head movement before trusted acceptance completes",\n    }\n    steps = job.get("steps")\n    if not isinstance(steps, list):\n        raise FinalGoError("trusted Xcode job lacks step evidence")\n    step_results = {\n        item.get("name"): item.get("conclusion")\n        for item in steps\n        if isinstance(item, dict) and isinstance(item.get("name"), str)\n    }\n    for name in required_steps:\n        _eq(step_results.get(name), "success", f"trusted Xcode step {name}")\n    run_url = job.get("run_url")\n    job_url = job.get("url")\n    if not isinstance(run_url, str) or not run_url.endswith(f"/actions/runs/{run_id}"):\n        raise FinalGoError("trusted Xcode job run URL does not bind the run ID")\n    if not isinstance(job_url, str) or not job_url.endswith(f"/actions/jobs/{job_id}"):\n        raise FinalGoError("trusted Xcode job URL does not bind the job ID")\n\n    artifact_raw, artifact = github_get_json(f"/actions/artifacts/{artifact_id}")\n    _eq(_positive_int(artifact.get("id"), "trusted Xcode artifact ID"), artifact_id, "trusted Xcode artifact ID")\n    expected_name = f"{TRUSTED_ARTIFACT_PREFIX}{expected_pr_number}-{run_number}-{run_attempt}"\n    _eq(artifact.get("name"), expected_name, "trusted Xcode artifact name")\n    _eq(artifact.get("expired"), False, "trusted Xcode artifact expiration state")\n    workflow_run = artifact.get("workflow_run")\n    if not isinstance(workflow_run, dict):\n        raise FinalGoError("trusted Xcode artifact metadata lacks workflow_run subject")\n    _eq(workflow_run.get("id"), run_id, "trusted Xcode artifact run ID")\n    _eq(workflow_run.get("head_sha"), workflow_source, "trusted Xcode artifact workflow source SHA")\n    archive_sha, archive_size = _sha_file(artifact_archive_path, "trusted Xcode artifact archive")\n    digest = artifact.get("digest")\n    if not isinstance(digest, str) or SHA256_DIGEST.fullmatch(digest) is None:\n        raise FinalGoError("trusted Xcode artifact lacks canonical server-declared SHA-256")\n    _eq(SHA256_DIGEST.fullmatch(digest).group(1), archive_sha, "trusted Xcode downloaded artifact archive digest")\n    declared_size = artifact.get("size_in_bytes")\n    if isinstance(declared_size, int) and not isinstance(declared_size, bool) and declared_size > 0:\n        _eq(declared_size, archive_size, "trusted Xcode downloaded artifact archive byte count")\n\n    embedded = _inspect_xcode_archive(artifact_archive_path, source)\n    return {\n        "authority": "default-branch-owner-command-v1",\n        "workflowName": TRUSTED_WORKFLOW_NAME,\n        "workflowPath": TRUSTED_WORKFLOW_PATH,\n        "workflowSourceCommitSHA": workflow_source,\n        "workflowGitBlob": workflow_blob,\n        "candidateSourceCommitSHA": source,\n        "prNumber": expected_pr_number,\n        "prAPIResponseSHA256": _sha(pr_raw),\n        "runID": run_id,\n        "runNumber": run_number,\n        "runAttempt": run_attempt,\n        "jobID": job_id,\n        "jobName": TRUSTED_JOB_NAME,\n        "runAPIResponseSHA256": _sha(run_raw),\n        "jobAPIResponseSHA256": _sha(job_raw),\n        "artifactID": artifact_id,\n        "artifactName": expected_name,\n        "artifactAPIResponseSHA256": _sha(artifact_raw),\n        "artifactArchiveSHA256": archive_sha,\n        "artifactArchiveByteCount": archive_size,\n        "embeddedExternalBuildRecordSHA256": embedded["sha256"],\n        "embeddedBuildIdentifier": embedded["buildIdentifier"],\n        "embeddedBuildInstanceID": embedded["buildInstanceID"],\n        "classification": "owner-commanded-default-branch-workflow-plus-exact-candidate-retained-artifact",\n    }\n\n\n'''
source = replace_span(
    source,
    "def _trusted_xcode_subject(",
    "def _inspect_xcode_archive",
    trusted_function,
    "trusted Xcode verifier",
)
source = replace_once(
    source,
    '''        artifact_archive_path=trusted_xcode_artifact_archive,\n        github_get_json=github_get_json,\n    )\n''',
    '''        artifact_archive_path=trusted_xcode_artifact_archive,\n        tooling_repo=tooling_repo,\n        github_get_json=github_get_json,\n    )\n''',
    "trusted verifier tooling repo wiring",
)

publication = '''def _fsync_directory(parent: Path) -> None:\n    directory_fd = os.open(parent, os.O_RDONLY)\n    try:\n        os.fsync(directory_fd)\n    finally:\n        os.close(directory_fd)\n\n\ndef _retract_published_record(output: Path, raw: bytes) -> None:\n    if not (output.exists() or output.is_symlink()):\n        _fsync_directory(output.parent)\n        return\n    if _regular(output, "failed published Final GO record") != raw:\n        raise FinalGoError(\n            "post-publication failure left changed destination bytes; refusing to delete unknown data"\n        )\n    output.unlink()\n    _fsync_directory(output.parent)\n    if output.exists() or output.is_symlink():\n        raise FinalGoError("post-publication rollback did not remove the authoritative destination")\n\n\ndef _quarantine_published_record(output: Path, raw: bytes) -> Path | None:\n    if not (output.exists() or output.is_symlink()):\n        _fsync_directory(output.parent)\n        return None\n    if _regular(output, "ambiguous published Final GO record") != raw:\n        raise FinalGoError("ambiguous Final GO destination bytes changed before quarantine")\n    quarantine = output.parent / (\n        f".{output.name}.QUARANTINED-NO-GO.{os.getpid()}.{hashlib.sha256(os.urandom(32)).hexdigest()[:16]}"\n    )\n    _publish_file_no_replace(output, quarantine)\n    _fsync_directory(output.parent)\n    if output.exists() or output.is_symlink():\n        raise FinalGoError("quarantine move did not remove the authoritative Final GO destination")\n    return quarantine\n\n\ndef publish_record_no_replace(\n    output_path: Path,\n    raw: bytes,\n    *,\n    publisher: Callable[[Path, Path], None] = _publish_file_no_replace,\n) -> str:\n    if not raw:\n        raise FinalGoError("Final GO record bytes must not be empty")\n    output = output_path.expanduser()\n    output.parent.mkdir(parents=True, exist_ok=True)\n    parent = output.parent.resolve(strict=True)\n    output = parent / output.name\n    if output.exists() or output.is_symlink():\n        raise FinalGoError(f"output already exists: {output}")\n    fd = -1\n    staging: Path | None = None\n    published = False\n    try:\n        fd, staging_name = tempfile.mkstemp(prefix=f".{output.name}.", suffix=".staging", dir=parent)\n        staging = Path(staging_name)\n        os.fchmod(fd, 0o600)\n        offset = 0\n        while offset < len(raw):\n            written = os.write(fd, raw[offset:])\n            if written <= 0:\n                raise OSError("short write while staging Final GO record")\n            offset += written\n        os.fsync(fd)\n        metadata = os.fstat(fd)\n        if not stat.S_ISREG(metadata.st_mode) or metadata.st_size != len(raw):\n            raise FinalGoError("staged Final GO record does not match expected byte count")\n        os.close(fd)\n        fd = -1\n        if _regular(staging, "staged Final GO record") != raw:\n            raise FinalGoError("staged Final GO record bytes changed before publication")\n        publisher(staging, output)\n        published = True\n        staging = None\n        _fsync_directory(parent)\n        if _regular(output, "published Final GO record") != raw:\n            raise FinalGoError("published Final GO record bytes differ from staged authority")\n        return _sha(raw)\n    except Exception as original_error:\n        if fd >= 0:\n            os.close(fd)\n        if not published and staging is not None:\n            try:\n                staging.unlink(missing_ok=True)\n            except OSError:\n                pass\n        if published:\n            try:\n                _retract_published_record(output, raw)\n            except Exception as rollback_error:\n                try:\n                    quarantine = _quarantine_published_record(output, raw)\n                except Exception as quarantine_error:\n                    raise FinalGoError(\n                        "Final GO publication failed after rename and durable rollback/quarantine "\n                        f"could not be proven. Treat {output} as AMBIGUOUS NO-GO and do not consume it. "\n                        f"rollback={rollback_error}; quarantine={quarantine_error}"\n                    ) from quarantine_error\n                note = f"; bytes quarantined at {quarantine}" if quarantine is not None else ""\n                raise FinalGoError(\n                    "Final GO publication failed after rename; authoritative destination was "\n                    f"removed but ordinary rollback was not proven{note}"\n                ) from rollback_error\n        raise original_error\n\n\n'''
source = replace_span(
    source,
    "def publish_record_no_replace(",
    "def _args(",
    publication,
    "failure-atomic publication",
)
SOURCE.write_text(source, encoding="utf-8")


tests = TESTS.read_text(encoding="utf-8")
tests = replace_once(
    tests,
    '    SOURCE = "a" * 40\n',
    '    SOURCE = "a" * 40\n    WORKFLOW_SOURCE = "f" * 40\n',
    "test workflow source constant",
)

make_github = '''    def make_github(self, archive_sha: str, archive_size: int):\n        pr = {\n            "number": self.PR,\n            "state": "open",\n            "head": {"sha": self.SOURCE, "repo": {"full_name": final_go.REPOSITORY}},\n            "base": {"repo": {"full_name": final_go.REPOSITORY}},\n        }\n        run = {\n            "id": self.RUN_ID,\n            "name": final_go.TRUSTED_WORKFLOW_NAME,\n            "path": final_go.TRUSTED_WORKFLOW_PATH,\n            "event": "issue_comment",\n            "head_sha": self.WORKFLOW_SOURCE,\n            "head_branch": final_go.DEFAULT_BRANCH,\n            "status": "completed",\n            "conclusion": "success",\n            "run_attempt": 1,\n            "run_number": 42,\n            "repository": {"full_name": final_go.REPOSITORY},\n            "head_repository": {"full_name": final_go.REPOSITORY},\n            "actor": {"login": final_go.REPOSITORY_OWNER},\n            "triggering_actor": {"login": final_go.REPOSITORY_OWNER},\n        }\n        required = [\n            "Reject stale or detached Capture head before scarce Mac work",\n            "Verify immutable trusted Capture head",\n            "Build, test, and capture Simulator states",\n            "Verify retained Capture evidence against trusted resolver authority",\n            "Reject head movement before trusted acceptance completes",\n        ]\n        job = {\n            "id": self.JOB_ID,\n            "run_id": self.RUN_ID,\n            "run_attempt": 1,\n            "workflow_name": final_go.TRUSTED_WORKFLOW_NAME,\n            "name": final_go.TRUSTED_JOB_NAME,\n            "head_sha": self.WORKFLOW_SOURCE,\n            "status": "completed",\n            "conclusion": "success",\n            "labels": ["xcode-27"],\n            "run_url": f"https://api.github.com/repos/{final_go.REPOSITORY}/actions/runs/{self.RUN_ID}",\n            "url": f"https://api.github.com/repos/{final_go.REPOSITORY}/actions/jobs/{self.JOB_ID}",\n            "steps": [{"name": name, "conclusion": "success"} for name in required],\n        }\n        artifact = {\n            "id": self.ARTIFACT_ID,\n            "name": f"{final_go.TRUSTED_ARTIFACT_PREFIX}{self.PR}-42-1",\n            "expired": False,\n            "digest": f"sha256:{archive_sha}",\n            "size_in_bytes": archive_size,\n            "workflow_run": {"id": self.RUN_ID, "head_sha": self.WORKFLOW_SOURCE},\n        }\n        records = {\n            f"/pulls/{self.PR}": pr,\n            f"/actions/runs/{self.RUN_ID}": run,\n            f"/actions/jobs/{self.JOB_ID}": job,\n            f"/actions/artifacts/{self.ARTIFACT_ID}": artifact,\n        }\n\n        def get(path: str):\n            value = records[path]\n            return json.dumps(value, sort_keys=True).encode(), value\n\n        return records, get\n\n'''
tests = replace_span(tests, "    def make_github(", "    def git_side_effect(", make_github, "test GitHub fixture")

git_side_effect = '''    def git_side_effect(self, repository: Path, *args: str):\n        subject = args[-1]\n        if subject == f"{self.SOURCE}^{{commit}}":\n            return self.SOURCE\n        if subject == f"{self.SOURCE}:{final_go.PRIVATE_RUNNER_PATH}":\n            return self.PRIVATE_BLOB\n        if subject == f"{self.SOURCE}:{final_go.INSPECTOR_PATH}":\n            return self.INSPECTOR_BLOB\n        if subject == f"{self.WORKFLOW_SOURCE}^{{commit}}":\n            return self.WORKFLOW_SOURCE\n        if subject == f"{self.WORKFLOW_SOURCE}:{final_go.TRUSTED_WORKFLOW_PATH}":\n            return final_go.TRUSTED_WORKFLOW_BLOB_SHA\n        if subject == f"{final_go.PINNED_CROSSCHECK_COMMIT}^{{commit}}":\n            return final_go.PINNED_CROSSCHECK_COMMIT\n        if subject == f"{final_go.PINNED_CROSSCHECK_COMMIT}:{final_go.CROSSCHECK_PATH}":\n            return self.TOOL_BLOB\n        raise AssertionError(args)\n\n'''
tests = replace_span(tests, "    def git_side_effect(", "    def setup_case(", git_side_effect, "test Git fixture")

tests = replace_span(
    tests,
    "    def test_rejects_non_pull_request_run_subject(self):",
    "    def test_rejects_skipped_exact_head_completion_step(self):",
    '''    def test_rejects_candidate_controlled_pull_request_run_subject(self):\n        with tempfile.TemporaryDirectory() as temporary:\n            _, records, kwargs = self.setup_case(Path(temporary))\n            run = records[f"/actions/runs/{self.RUN_ID}"]\n            run["event"] = "pull_request"\n            run["name"] = "Xcode 27 PR Exact-Head QA"\n            run["path"] = ".github/workflows/xcode27-pr-command.yml"\n            run["head_sha"] = self.SOURCE\n            with self.assertRaisesRegex(final_go.FinalGoError, "workflow name mismatch"):\n                self.build(kwargs)\n\n''',
    "candidate-controlled workflow regression",
)
tests = replace_once(
    tests,
    '"Reject head movement before acceptance completion mismatch"',
    '"Reject head movement before trusted acceptance completes mismatch"',
    "trusted final step assertion",
)

additional_trust_tests = '''    def test_rejects_live_pr_head_movement(self):\n        with tempfile.TemporaryDirectory() as temporary:\n            _, records, kwargs = self.setup_case(Path(temporary))\n            records[f"/pulls/{self.PR}"]["head"]["sha"] = "0" * 40\n            with self.assertRaisesRegex(final_go.FinalGoError, "live Capture PR head SHA mismatch"):\n                self.build(kwargs)\n\n    def test_rejects_non_owner_trusted_command_actor(self):\n        with tempfile.TemporaryDirectory() as temporary:\n            _, records, kwargs = self.setup_case(Path(temporary))\n            records[f"/actions/runs/{self.RUN_ID}"]["actor"] = {"login": "not-owner"}\n            with self.assertRaisesRegex(final_go.FinalGoError, "command actor"):\n                self.build(kwargs)\n\n    def test_rejects_unpinned_trusted_workflow_blob(self):\n        with tempfile.TemporaryDirectory() as temporary:\n            _, _, kwargs = self.setup_case(Path(temporary))\n\n            def drifted_git(repository: Path, *args: str):\n                if args[-1] == f"{self.WORKFLOW_SOURCE}:{final_go.TRUSTED_WORKFLOW_PATH}":\n                    return "9" * 40\n                return self.git_side_effect(repository, *args)\n\n            with mock.patch.object(final_go, "_git", side_effect=drifted_git):\n                with self.assertRaisesRegex(final_go.FinalGoError, "workflow Git blob mismatch"):\n                    final_go.build_final_go_record(**kwargs)\n\n'''
tests = replace_once(
    tests,
    "    def test_rejects_downloaded_artifact_substitution(self):\n",
    additional_trust_tests + "    def test_rejects_downloaded_artifact_substitution(self):\n",
    "additional trusted-workflow regressions",
)

post_publish_test = '''    def test_post_publish_directory_fsync_failure_retracts_final_record(self):\n        with tempfile.TemporaryDirectory() as temporary:\n            root = Path(temporary)\n            output = root / "FinalGO.json"\n            raw = b'{"decision":"GO"}\\n'\n            real_fsync = final_go.os.fsync\n            calls = 0\n\n            def fail_second_fsync(fd: int):\n                nonlocal calls\n                calls += 1\n                if calls == 2:\n                    raise OSError("injected post-publication directory fsync failure")\n                return real_fsync(fd)\n\n            with mock.patch.object(final_go.os, "fsync", side_effect=fail_second_fsync):\n                with self.assertRaisesRegex(OSError, "post-publication"):\n                    final_go.publish_record_no_replace(output, raw)\n            self.assertFalse(output.exists() or output.is_symlink())\n            self.assertEqual(list(root.glob(".FinalGO.json.*.staging")), [])\n\n'''
tests = replace_once(
    tests,
    "    def test_publication_refuses_existing_final_record(self):\n",
    post_publish_test + "    def test_publication_refuses_existing_final_record(self):\n",
    "post-publication rollback regression",
)
TESTS.write_text(tests, encoding="utf-8")

workflow = WORKFLOW.read_text(encoding="utf-8")
workflow = replace_span(
    workflow,
    "      - name: Verify current trusted workflow and Research tuple are pinned exactly",
    "\u0000",
    '''      - name: Verify trusted default-branch workflow and Research tuple are pinned exactly\n        run: |\n          set -euo pipefail\n          grep -Fq 'TRUSTED_WORKFLOW_NAME = "Capture Trusted Xcode 27 Exact-Head QA"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'TRUSTED_WORKFLOW_PATH = ".github/workflows/capture-xcode27-trusted-command.yml"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'TRUSTED_WORKFLOW_BLOB_SHA = "e5ef72f50bed279e98ad28b94930831b747d0c20"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'TRUSTED_JOB_NAME = "Build, test, and capture trusted exact Capture head"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'PINNED_CROSSCHECK_COMMIT = "d827a296048386bda62024ea3278775d5344c47c"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'PINNED_CROSSCHECK_BLOB = "c3b2b620280484c05316fc5c2fa2ca451f1fdc83"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'RESEARCH_COMPILE_MODE = "private-today-v1"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'RESEARCH_COMPILE_AUTHORITY = "canonical-producer-explicit-mode"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'RESEARCH_COMPILE_CONDITION = "NEMBRA_ES80_TODAY_RESEARCH"' scripts/ci/es80_today_final_go_record.py\n          grep -Fq 'name: Capture Trusted Xcode 27 Exact-Head QA' .github/workflows/capture-xcode27-trusted-command.yml\n          grep -Fq 'name: Build, test, and capture trusted exact Capture head' .github/workflows/capture-xcode27-trusted-command.yml\n''',
    "Final GO QA trust pin",
)
WORKFLOW.write_text(workflow, encoding="utf-8")
