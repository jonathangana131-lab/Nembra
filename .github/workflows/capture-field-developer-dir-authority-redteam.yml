name: Capture Field Developer Directory Authority Red Team

on:
  pull_request:
    paths:
      - scripts/field/install_one_time_capture.command
      - scripts/ci/tests/test_capture_field_developer_dir_authority.py
      - .github/workflows/capture-field-developer-dir-authority-redteam.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  developer-directory-authority:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          fetch-depth: 0
      - name: Bind exact diagnostic checkout
        shell: bash
        run: |
          set -euo pipefail
          expected='${{ github.event.pull_request.head.sha || github.sha }}'
          test "$(git rev-parse HEAD)" = "$expected"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
      - name: Require closed developer-directory authority
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 -m py_compile scripts/ci/tests/test_capture_field_developer_dir_authority.py
          /usr/bin/python3 scripts/ci/tests/test_capture_field_developer_dir_authority.py
          /bin/bash -n scripts/field/install_one_time_capture.command
          git diff --check
