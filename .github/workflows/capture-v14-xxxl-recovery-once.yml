name: Capture V14 Accessibility XXXL recovery

on:
  push:
    branches:
      - agent/capture-nogo-xxxl-viewport
    paths:
      - .github/workflows/capture-v14-xxxl-recovery-once.yml

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact recovery branch
        uses: actions/checkout@v5
        with:
          ref: agent/capture-nogo-xxxl-viewport
          fetch-depth: 0

      - name: Recompose locked Capture surface for Accessibility XXXL
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          path = Path("NembraApp/App/NembraApp.swift")
          source = path.read_text(encoding="utf-8")

          replacements = [
              (
                  "spacing: verticalSizeClass == .compact ? 10 : (isAccessibilityLayout ? 8 : 28)",
                  "spacing: verticalSizeClass == .compact ? 10 : (isAccessibilityLayout ? 6 : 28)",
              ),
              (
                  "spacing: verticalSizeClass == .compact ? 6 : (isAccessibilityLayout ? 4 : 14)",
                  "spacing: verticalSizeClass == .compact ? 6 : (isAccessibilityLayout ? 2 : 14)",
              ),
              (
                  "isAccessibilityLayout || verticalSizeClass == .compact ? .title2 : .largeTitle,",
                  "isAccessibilityLayout ? .headline : (verticalSizeClass == .compact ? .title2 : .largeTitle),",
              ),
              (
                  """Text(\"Final checks in progress.\")
                            .font(.caption.weight(.semibold))""",
                  """Text(\"Final checks in progress.\")
                            .font(.caption2.weight(.semibold))""",
              ),
              (
                  """Text(isAccessibilityLayout ? \"PHYSICAL CAPTURE · NO-GO\" : \"Not ready for scooter capture yet\")
                            .font(.headline)""",
                  """Text(isAccessibilityLayout ? \"PHYSICAL CAPTURE · NO-GO\" : \"Not ready for scooter capture yet\")
                            .font(isAccessibilityLayout ? .subheadline.weight(.semibold) : .headline)""",
              ),
              (
                  """Text(\"Engineering details\")
                                    .font(.subheadline.weight(.semibold))""",
                  """Text(\"Engineering details\")
                                    .font(isAccessibilityLayout ? .footnote.weight(.semibold) : .subheadline.weight(.semibold))""",
              ),
              (
                  ".padding(.top, verticalSizeClass == .compact ? 8 : (isAccessibilityLayout ? 8 : 18))",
                  ".padding(.top, verticalSizeClass == .compact ? 8 : (isAccessibilityLayout ? 4 : 18))",
              ),
          ]

          for old, new in replacements:
              actual_count = source.count(old)
              if actual_count != 1:
                  raise SystemExit(
                      f"Refusing ambiguous recovery for {old!r}: expected 1, found {actual_count}"
                  )
              source = source.replace(old, new, 1)

          def replace_nearest_padding_before(marker: str, old: str, new: str) -> None:
              global source
              marker_pos = source.find(marker)
              if marker_pos < 0:
                  raise SystemExit(f"Missing recovery marker: {marker}")
              padding_pos = source.rfind(old, 0, marker_pos)
              if padding_pos < 0:
                  raise SystemExit(f"Missing expected padding before marker: {marker}")
              if source.find(old, padding_pos + len(old), marker_pos) >= 0:
                  raise SystemExit(f"Ambiguous padding before marker: {marker}")
              source = source[:padding_pos] + new + source[padding_pos + len(old):]

          old_card_padding = ".padding(verticalSizeClass == .compact ? 12 : (isAccessibilityLayout ? 10 : 18))"
          new_card_padding = ".padding(verticalSizeClass == .compact ? 12 : (isAccessibilityLayout ? 8 : 18))"
          replace_nearest_padding_before(
              '.accessibilityIdentifier("es80.capture.physical-run-locked")',
              old_card_padding,
              new_card_padding,
          )
          replace_nearest_padding_before(
              '.background(.white.opacity(0.045), in: RoundedRectangle(cornerRadius: 20, style: .continuous))',
              old_card_padding,
              new_card_padding,
          )

          path.write_text(source, encoding="utf-8")
          PY

          git diff --check
          git diff -- NembraApp/App/NembraApp.swift

      - name: Commit recovery and remove one-shot workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "nembra-v14-closure-bot"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm .github/workflows/capture-v14-xxxl-recovery-once.yml
          git add NembraApp/App/NembraApp.swift
          git diff --cached --check
          git commit -m "fix(capture): recompose locked state for Accessibility XXXL"
          git push origin HEAD:"$GITHUB_REF_NAME"
