name: V14 Recoverable Experiment One admission

on:
  push:
    branches:
      - feature/es80-v14-recoverable-admission-current/gpt56-thinking-r6

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Apply product repair
        run: python3 scripts/ci/v14_recoverable_admission_patch.py
      - name: Validate and publish product tree
        run: |
          set -euo pipefail
          git diff --check
          rm .github/workflows/v14-recoverable-experiment-one-admission.yml
          rm scripts/ci/v14_recoverable_admission_patch.py
          git add -A
          git diff --cached --check
          git config user.name nembra-v14-gpt56
          git config user.email actions@users.noreply.github.com
          git commit -m "Capture: preserve admission across recoverable rediscovery"
          git push origin HEAD:feature/es80-v14-recoverable-admission-current/gpt56-thinking-r6
