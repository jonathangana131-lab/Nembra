name: Capture carrier authority v3 isolated child

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches:
      - product/capture-1-0-main-20260818

permissions:
  contents: write
  pull-requests: read

concurrency:
  group: capture-carrier-authority-v3-child-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  materialize:
    if: >-
      github.event.pull_request.head.repo.full_name == github.repository &&
      github.event.pull_request.head.ref == 'recovery/capture-authority-v3-materialized-gpt56sol-20260819'
    runs-on: xcode-27
    timeout-minutes: 35
    env:
      TARGET_BRANCH: recovery/capture-authority-v3-materialized-gpt56sol-20260819
      APP_SOURCE: NembraApp/App/NembraCaptureEntrypoint.swift
      AUTHORING_WORKFLOW: .github/workflows/capture-carrier-authority-v2-materialize-once.yml
      RECOVERY_MARKER: .github/capture-authority-v3-recovery-subject.txt
      EXPECTED_AUTHORING_BLOB: 59d38ec605728302604ed2b3321b6bd4558dbdda
      EXPECTED_SOURCE_BLOB: 627c17949d15aaf26ca28c12abb9bb684ab0e731
    steps:
      - name: Checkout immutable recovery head
        uses: actions/checkout@v6
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          fetch-depth: 0
          persist-credentials: true

      - name: Bind exact reviewed subject or exact materialized descendant
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
          test "$(git rev-parse HEAD:"$AUTHORING_WORKFLOW")" = "$EXPECTED_AUTHORING_BLOB"
          if test -f "$RECOVERY_MARKER"; then
            test "$(git rev-parse HEAD:"$APP_SOURCE")" = "$EXPECTED_SOURCE_BLOB"
            printf 'CAPTURE_V3_MODE=materialize\n' >> "$GITHUB_ENV"
          else
            read -r head parent extra <<<"$(git rev-list --parents -n 1 HEAD)"
            test -n "$parent"
            test -z "${extra:-}"
            test "$(git rev-parse "$parent:$AUTHORING_WORKFLOW")" = "$EXPECTED_AUTHORING_BLOB"
            test "$(git rev-parse "$parent:$APP_SOURCE")" = "$EXPECTED_SOURCE_BLOB"
            git cat-file -e "$parent:$RECOVERY_MARKER"
            changed="$(git diff-tree --no-commit-id --name-only -r HEAD | sort)"
            expected="$(printf '%s\n%s\n' "$APP_SOURCE" "$RECOVERY_MARKER" | sort)"
            test "$changed" = "$expected"
            printf 'CAPTURE_V3_MODE=verify\nCAPTURE_V3_PARENT=%s\n' "$parent" >> "$GITHUB_ENV"
          fi
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

      - name: Apply or deterministically reconstruct reviewed V2 transform plus V3 corrections
        shell: python
        run: |
          import os
          import subprocess
          from pathlib import Path

          path = Path("NembraApp/App/NembraCaptureEntrypoint.swift")
          mode = os.environ["CAPTURE_V3_MODE"]
          final_bytes = None
          if mode == "verify":
              final_bytes = path.read_bytes()
              parent = os.environ["CAPTURE_V3_PARENT"]
              path.write_bytes(subprocess.check_output(["git", "show", f"{parent}:{path.as_posix()}"]))
          elif mode != "materialize":
              raise SystemExit(f"unexpected Capture V3 mode: {mode}")

          authoring = Path(".github/workflows/capture-carrier-authority-v2-materialize-once.yml").read_text(encoding="utf-8")
          start = '          cat > "${RUNNER_TEMP}/materialize_capture_authority_v2.py" <<\'PY\'\n'
          end = '          PY\n\n      - name: Materialize against latest reviewed carrier source\n'
          if authoring.count(start) != 1 or authoring.count(end) != 1:
              raise SystemExit("reviewed V2 transform markers changed; refusing")
          body = authoring.split(start, 1)[1].split(end, 1)[0]
          lines = body.splitlines()
          if any(line and not line.startswith("          ") for line in lines):
              raise SystemExit("unexpected V2 transform indentation; refusing")
          script = "\n".join(line[10:] if line else "" for line in lines) + "\n"
          exec(compile(script, "capture-authority-v2", "exec"), {"__name__": "__main__"})

          source = path.read_text(encoding="utf-8")
          old_failed = '''            if phase == .failed {\n                fieldAuthorization.revoke()\n                operatorSafetyAttemptID = nil\n'''
          new_failed = '''            if phase == .failed {\n                if fieldAuthorization.stage != .armed {\n                    fieldAuthorization.revoke()\n                }\n                operatorSafetyAttemptID = nil\n'''
          if source.count(old_failed) != 1:
              raise SystemExit(f"armed-preservation anchor count was {source.count(old_failed)}; refusing")
          source = source.replace(old_failed, new_failed, 1)

          old_seal_failure = '''                            self.currentConnectionToken = nil\n                            self.localBLESettlementToken = nil\n                            self.sdkLocalBLEOnline = false\n                            self.driver = nil\n                            self.fieldAuthorization.revoke()\n                            self.phase = .failed\n                            self.message = "Accepted observation could not freeze and seal one exact authorized artifact: \\(error.localizedDescription). Relaunch Capture before another attempt."\n                            self.log("field_authorization_exact_artifact_seal_failed", [\n                                "generation": String(token.diagnosticGeneration)\n                            ])\n                            return\n'''
          new_seal_failure = '''                            self.currentConnectionToken = nil\n                            self.localBLESettlementToken = nil\n                            self.sdkLocalBLEOnline = false\n                            self.driver = nil\n                            self.fieldAuthorization.revoke()\n                            self.failLocally(\n                                "Accepted observation could not freeze and seal one exact authorized artifact: \\(error.localizedDescription). Relaunch Capture before another attempt.",\n                                "field_authorization_exact_artifact_seal_failed"\n                            )\n                            return\n'''
          if source.count(old_seal_failure) != 1:
              raise SystemExit(f"seal-failure lifecycle anchor count was {source.count(old_seal_failure)}; refusing")
          source = source.replace(old_seal_failure, new_seal_failure, 1)
          path.write_text(source, encoding="utf-8")

          if mode == "verify":
              reconstructed = path.read_bytes()
              if reconstructed != final_bytes:
                  raise SystemExit("materialized app source does not exactly match deterministic transform of pinned parent")
              path.write_bytes(final_bytes)

      - name: Verify diagnostic source shape
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git status --porcelain=v1 | awk '{print $2}' | sort)"
          if test "$CAPTURE_V3_MODE" = materialize; then
            test "$changed" = "$APP_SOURCE"
          else
            test -z "$changed"
          fi
          git diff --check

      - name: Verify lifecycle Python contract
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 -I scripts/ci/tests/test_es80_app_authorization_lifecycle_wiring.py

      - name: Verify package app-session contract
        if: always()
        working-directory: Packages/NembraBluetoothCapture
        run: swift test --filter AuthenticatedStationaryCaptureAppSessionTests

      - name: Verify app-wiring source contract
        if: always()
        working-directory: Packages/NembraBluetoothCapture
        run: swift test --filter CaptureSimulatorQAHarnessSourceTests_AuthenticatedFieldCapabilityAppWiring

      - name: Verify inbox-adapter source contract
        if: always()
        working-directory: Packages/NembraBluetoothCapture
        run: swift test --filter CaptureSimulatorQAHarnessSourceTests_AuthorizationInboxAppAdapter

      - name: Verify exact artifact-freeze source contract
        if: always()
        working-directory: Packages/NembraBluetoothCapture
        run: swift test --filter TuyaAuthenticatedFieldCapabilityArtifactFreezeSourceTests

      - name: Verify no authority bypasses
        if: always()
        shell: bash
        run: |
          set -euo pipefail
          grep -Fq 'advanceInboxHandoffIfAvailable()' "$APP_SOURCE"
          grep -Fq 'fieldAuthorization.stage == .armed' "$APP_SOURCE"
          grep -Fq 'fieldAuthorization.stage != .armed' "$APP_SOURCE"
          grep -Fq 'try self.fieldAuthorization.sealAfterAcceptedArtifactFreeze()' "$APP_SOURCE"
          test "$(grep -Fc 'isAuthoritativeFieldBuild = true' "$APP_SOURCE")" = 0
          test "$(grep -Fc 'AuthenticatedStationaryCaptureAttemptCapability' "$APP_SOURCE")" = 0
          test "$(grep -Fc 'AuthenticatedStationaryCaptureCapabilityGate' "$APP_SOURCE")" = 0

      - name: Build standalone Capture on Xcode 27
        shell: bash
        run: |
          set -euo pipefail
          xcodebuild -version
          xcodebuild \
            -project NembraCapture.xcodeproj \
            -scheme 'Nembra Capture' \
            -configuration Debug \
            -destination 'generic/platform=iOS Simulator' \
            -derivedDataPath "${RUNNER_TEMP}/NembraCaptureCarrierAuthorityV3Child" \
            CODE_SIGNING_ALLOWED=NO \
            build

      - name: Publish only the exact tested recovery descendant
        if: env.CAPTURE_V3_MODE == 'materialize'
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin "$TARGET_BRANCH"
          test "$(git rev-parse FETCH_HEAD)" = "${{ github.event.pull_request.head.sha }}"
          rm "$RECOVERY_MARKER"
          git add -- "$APP_SOURCE" "$RECOVERY_MARKER"
          staged="$(git diff --cached --name-only | sort)"
          expected="$(printf '%s\n%s\n' "$APP_SOURCE" "$RECOVERY_MARKER" | sort)"
          test "$staged" = "$expected"
          git diff --cached --check
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m 'feat(capture): wire authorized SecureLink lifecycle v3'
          materialized="$(git rev-parse HEAD)"
          git fetch origin "$TARGET_BRANCH"
          test "$(git rev-parse FETCH_HEAD)" = "${{ github.event.pull_request.head.sha }}"
          git push origin HEAD:"$TARGET_BRANCH"
          printf 'CAPTURE_AUTHORITY_V3_CHILD_MATERIALIZED=%s trigger=%s physicalAuthorityCreated=false\n' "$materialized" "${{ github.event.pull_request.head.sha }}"

      - name: Confirm exact-head verification mode
        if: env.CAPTURE_V3_MODE == 'verify'
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
          printf 'CAPTURE_AUTHORITY_V3_CHILD_EXACT_HEAD_VERIFIED=%s parent=%s physicalAuthorityCreated=false\n' "${{ github.event.pull_request.head.sha }}" "$CAPTURE_V3_PARENT"
