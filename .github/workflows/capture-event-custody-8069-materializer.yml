name: Capture event custody materializer

on:
  push:
    branches:
      - repair/v14-capture-event-custody-8069-sol

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Materialize exact-current event custody repair
        run: python3 .github/materializers/capture_event_custody_8069.py
