name: Capture App Internal Lifecycle Patch

on:
  push:
    branches:
      - agent/v14-capture-app-internal-lifecycle-consumption-sol
    paths:
      - .github/workflows/capture-app-internal-lifecycle-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v14-capture-app-internal-lifecycle-consumption-sol
          fetch-depth: 0
      - name: Apply production repair and self-retire
        shell: bash
        run: |
          set -euo pipefail
          python3 Scripts/apply_capture_internal_lifecycle_app_patch.py
          rm Scripts/apply_capture_internal_lifecycle_app_patch.py
          rm .github/workflows/capture-app-internal-lifecycle-patch.yml
          git config user.name nembra-v14-integration
          git config user.email actions@users.noreply.github.com
          git add -A
          git diff --cached --check
          git diff --cached -- NembraApp/App/NembraCaptureEntrypoint.swift | grep -q 'markInternalLifecycleFailure'
          git diff --cached -- NembraApp/App/NembraCaptureEntrypoint.swift | grep -q 'MutationError.monotonicClockRegressed'
          git commit -m "fix(capture): consume no-clock app lifecycle terminal"
          git push origin HEAD:agent/v14-capture-app-internal-lifecycle-consumption-sol
