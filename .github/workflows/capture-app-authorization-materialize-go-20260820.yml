name: Capture app authorization materialize GO (retired)

# RETIRED: this legacy publisher is intentionally non-authorizing and non-writing.
# The carrier-native `capture-app-authorization-race-safe-go.yml` is the sole
# production-source materializer because it applies the reviewed root bootstrap,
# stage-aware readiness, lifecycle oracle, adversarial suites, and Xcode 27 gate.
# Keeping this tombstone briefly makes any stale/manual invocation fail closed.

on:
  push:
    branches:
      - product/capture-1-0-main-20260818
    paths:
      - .github/workflows/capture-app-authorization-materialize-go-20260820.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse legacy publication path
        shell: bash
        run: |
          set -euo pipefail
          echo 'This Capture authorization publisher is retired.' >&2
          echo 'Use capture-app-authorization-race-safe-go.yml; no source or physical authority was created.' >&2
          exit 1
