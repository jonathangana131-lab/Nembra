name: Capture auth promotion materializer
on:
  push:
    branches: [repair/v14-capture-auth-promotion-reentrancy-a7-sol]
permissions:
  contents: write
jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 3
      - run: python3 .github/materializers/capture_auth_promotion_reentrancy_a7_sol.py
