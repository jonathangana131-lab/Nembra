name: Materialize Capture Application Secret Redaction Current Sol2

on:
  push:
    branches:
      - "fix/v14-capture-app-update-secret-redaction-current-sol2"

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Verify exact product parent
        shell: bash
        run: |
          set -euo pipefail
          expected="52333deb09ea9a5964250097f83884d285f25d78"
          actual="$(git rev-parse HEAD^)"
          test "$actual" = "$expected" || { echo "Expected parent $expected, got $actual" >&2; exit 1; }

      - name: Materialize product repair and regression
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          source_path = Path("NembraApp/App/NembraCaptureEntrypoint.swift")
          source = source_path.read_text()
          old = '''    func device(_ device: ThingSmartDevice?, dpsUpdate dps: [AnyHashable: Any]?) {
              guard let dps, !dps.isEmpty else { return }
              var sanitized: [String: String] = [:]
              for (key, value) in dps {
                  sanitized[String(describing: key)] = String(describing: value)
              }
              onApplicationUpdate?(sanitized)
          }
          '''
          new = '''    private static let secretKeyFragments = [
              "localkey",
              "accesstoken",
              "refreshtoken",
              "authkey",
              "seckey",
          ]

          private static func isCredentialShapedKey(_ key: String) -> Bool {
              let normalizedKey = key.lowercased().filter { $0.isLetter || $0.isNumber }
              return secretKeyFragments.contains { normalizedKey.contains($0) }
          }

          private static func redactApplicationSecrets(_ object: Any) -> Any {
              if let dictionary = object as? [AnyHashable: Any] {
                  var sanitized: [String: Any] = [:]
                  sanitized.reserveCapacity(dictionary.count)
                  for (key, value) in dictionary {
                      let keyString = String(describing: key)
                      if isCredentialShapedKey(keyString) {
                          sanitized[keyString] = "<redacted>"
                      } else {
                          sanitized[keyString] = redactApplicationSecrets(value)
                      }
                  }
                  return sanitized
              }
              if let array = object as? [Any] {
                  return array.map(redactApplicationSecrets)
              }
              return object
          }

          func device(_ device: ThingSmartDevice?, dpsUpdate dps: [AnyHashable: Any]?) {
              guard let dps, !dps.isEmpty else { return }
              var sanitized: [String: String] = [:]
              sanitized.reserveCapacity(dps.count)
              for (key, value) in dps {
                  let keyString = String(describing: key)
                  if Self.isCredentialShapedKey(keyString) {
                      sanitized[keyString] = "<redacted>"
                  } else {
                      sanitized[keyString] = String(describing: Self.redactApplicationSecrets(value))
                  }
              }
              onApplicationUpdate?(sanitized)
          }
          '''
          if source.count(old) != 1:
              raise SystemExit(f"expected exactly one direct application projection block, found {source.count(old)}")
          source_path.write_text(source.replace(old, new))

          test_path = Path("Packages/NembraBluetoothCapture/Tests/NembraBluetoothCaptureTests/TuyaApplicationUpdateSecretRedactionSourceTests.swift")
          if test_path.exists():
              raise SystemExit(f"refusing to overwrite existing {test_path}")
          test_path.write_text(r'''import Foundation
          import Testing
          @testable import NembraBluetoothCapture

          @Suite("Tuya authenticated application update secret redaction source contract")
          struct TuyaApplicationUpdateSecretRedactionSourceTests {
              @Test("authenticated application update details are recursively redacted before controller custody")
              func applicationUpdateCannotRetainCredentialShapedValues() throws {
                  let source = try readRepositoryFile("NembraApp/App/NembraCaptureEntrypoint.swift")
                  let driver = String(try section(
                      in: source,
                      from: "@MainActor\nprivate final class SmartLifeDriver",
                      to: "#endif\n\nprivate enum AppleAccountAuthorizationError"
                  ))

                  #expect(driver.contains("private static func redactApplicationSecrets(_ object: Any) -> Any"))
                  #expect(driver.contains("secretKeyFragments"))
                  #expect(driver.contains("localkey"))
                  #expect(driver.contains("accesstoken"))
                  #expect(driver.contains("refreshtoken"))
                  #expect(driver.contains("authkey"))
                  #expect(driver.contains("seckey"))
                  #expect(driver.contains("key.lowercased().filter"))
                  #expect(driver.contains("$0.isLetter || $0.isNumber"))
                  #expect(driver.contains("array.map(redactApplicationSecrets)"))
                  #expect(driver.contains("String(describing: Self.redactApplicationSecrets(value))"))
                  #expect(driver.contains("sanitized[keyString] = \"<redacted>\""))
                  #expect(driver.contains("onApplicationUpdate?(sanitized)"))
                  #expect(!driver.contains("sanitized[String(describing: key)] = String(describing: value)"))
              }

              @Test("export redaction claim remains coupled to the application-event path")
              func exportRedactionClaimIncludesApplicationEvents() throws {
                  let source = try readRepositoryFile("NembraApp/App/NembraCaptureEntrypoint.swift")
                  let export = String(try section(
                      in: source,
                      from: "private func makeExport(exportedAt:",
                      to: "func prepareExport()"
                  ))
                  let updates = String(try section(
                      in: source,
                      from: "private func receivedApplicationUpdate(",
                      to: "private func startWatchdog"
                  ))

                  #expect(export.contains("secretsRedacted: true"))
                  #expect(updates.contains("log(\"tuya_application_update\", update.merging(["))
              }

              private func section(in source: String, from start: String, to end: String) throws -> Substring {
                  guard let startRange = source.range(of: start),
                        let endRange = source.range(of: end, range: startRange.upperBound..<source.endIndex) else {
                      Issue.record("Expected source section missing: \(start) ... \(end)")
                      throw SourceContractError.sectionMissing
                  }
                  return source[startRange.lowerBound..<endRange.lowerBound]
              }

              private func readRepositoryFile(_ relativePath: String) throws -> String {
                  let repositoryRoot = URL(fileURLWithPath: #filePath)
                      .deletingLastPathComponent()
                      .deletingLastPathComponent()
                      .deletingLastPathComponent()
                      .deletingLastPathComponent()
                      .deletingLastPathComponent()
                  return try String(contentsOf: repositoryRoot.appendingPathComponent(relativePath), encoding: .utf8)
              }

              private enum SourceContractError: Error {
                  case sectionMissing
              }
          }
          ''')
          PY

      - name: Validate syntax and redaction invariants
        shell: bash
        run: |
          set -euo pipefail
          swiftc -frontend -parse NembraApp/App/NembraCaptureEntrypoint.swift
          swiftc -frontend -parse Packages/NembraBluetoothCapture/Tests/NembraBluetoothCaptureTests/TuyaApplicationUpdateSecretRedactionSourceTests.swift
          python3 - <<'PY'
          from pathlib import Path
          source = Path("NembraApp/App/NembraCaptureEntrypoint.swift").read_text()
          start = source.index("@MainActor\nprivate final class SmartLifeDriver")
          end = source.index("#endif\n\nprivate enum AppleAccountAuthorizationError", start)
          driver = source[start:end]
          required = [
              "private static func redactApplicationSecrets(_ object: Any) -> Any",
              "secretKeyFragments",
              '"localkey"', '"accesstoken"', '"refreshtoken"', '"authkey"', '"seckey"',
              "key.lowercased().filter { $0.isLetter || $0.isNumber }",
              "return array.map(redactApplicationSecrets)",
              "String(describing: Self.redactApplicationSecrets(value))",
              'sanitized[keyString] = "<redacted>"',
              "onApplicationUpdate?(sanitized)",
          ]
          missing = [token for token in required if token not in driver]
          if missing:
              raise SystemExit(f"missing redaction invariants: {missing}")
          forbidden = "sanitized[String(describing: key)] = String(describing: value)"
          if forbidden in driver:
              raise SystemExit("direct unsanitized application projection remains")
          if "secretsRedacted: true" not in source:
              raise SystemExit("export redaction declaration unexpectedly absent")
          if 'log("tuya_application_update", update.merging([' not in source:
              raise SystemExit("application-event custody path unexpectedly absent")
          PY
          cat > /tmp/redaction_probe.swift <<'SWIFT'
          import Foundation

          let secretKeyFragments = ["localkey", "accesstoken", "refreshtoken", "authkey", "seckey"]
          func isCredentialShapedKey(_ key: String) -> Bool {
              let normalizedKey = key.lowercased().filter { $0.isLetter || $0.isNumber }
              return secretKeyFragments.contains { normalizedKey.contains($0) }
          }
          func redactApplicationSecrets(_ object: Any) -> Any {
              if let dictionary = object as? [AnyHashable: Any] {
                  var sanitized: [String: Any] = [:]
                  for (key, value) in dictionary {
                      let keyString = String(describing: key)
                      sanitized[keyString] = isCredentialShapedKey(keyString) ? "<redacted>" : redactApplicationSecrets(value)
                  }
                  return sanitized
              }
              if let array = object as? [Any] { return array.map(redactApplicationSecrets) }
              return object
          }
          precondition(isCredentialShapedKey("local_key"))
          precondition(isCredentialShapedKey("refresh-token"))
          precondition(isCredentialShapedKey("AUTH.key"))
          precondition(!isCredentialShapedKey("speed"))
          let nested: [AnyHashable: Any] = [
              "speed": 17,
              "payload": [["access_token": "secret", "battery": 72]],
              "local_key": "root-secret",
          ]
          let output = redactApplicationSecrets(nested) as! [String: Any]
          precondition(output["local_key"] as? String == "<redacted>")
          let payload = output["payload"] as! [Any]
          let first = payload[0] as! [String: Any]
          precondition(first["access_token"] as? String == "<redacted>")
          precondition(first["battery"] as? Int == 72)
          precondition(output["speed"] as? Int == 17)
          SWIFT
          swift /tmp/redaction_probe.swift
          git diff --check

      - name: Commit durable repair
        shell: bash
        run: |
          set -euo pipefail
          rm .github/workflows/materialize-capture-app-update-secret-redaction-current-sol2.yml
          git config user.name "Nembra V14 Sol"
          git config user.email "actions@users.noreply.github.com"
          git add NembraApp/App/NembraCaptureEntrypoint.swift \
            Packages/NembraBluetoothCapture/Tests/NembraBluetoothCaptureTests/TuyaApplicationUpdateSecretRedactionSourceTests.swift \
            .github/workflows/materialize-capture-app-update-secret-redaction-current-sol2.yml
          git diff --cached --check
          git commit -m "fix(capture): redact authenticated application secrets"
          git push origin HEAD:fix/v14-capture-app-update-secret-redaction-current-sol2
