# V17 clean recovery materializer. Branches away from occupied #3553 and self-retires.
name: Capture native ACL fail-closed recovery one-shot

on:
  push:
    branches:
      - recovery/v17-capture-native-acl-clean-product-gpt56sol-20260817
    paths:
      - .github/workflows/capture-native-acl-failclosed-one-shot.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bind exact recovery branch
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = "recovery/v17-capture-native-acl-clean-product-gpt56sol-20260817"
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test -z "$(git status --porcelain)"

      - name: Materialize coherent fail-closed product repair
        shell: python
        run: |
          from pathlib import Path

          orchestrator = Path("scripts/ci/capture_selected_xcode_build_orchestrator.py")
          text = orchestrator.read_text()
          old = '''                        # Path replacement is not a rollback failure: descriptor custody
                        # intentionally restores the original vnode. Verify path text only
                        # when the canonical name still identifies that exact object.
                        if path_matches:
                            restored = _path_acl_listing(
                                path, descriptor, accepted_signature, is_directory
                            )
                            if restored != str(record["before"]):
                                failures.append(
                                    f"private read-lease did not restore exact ACL listing: {path}"
                                )
'''
          new = '''                        # Descriptor custody restores the original vnode even if the
                        # canonical name was replaced, but namespace identity loss during an
                        # authority-bearing lease is still terminal. Never mutate the replacement.
                        if not path_matches:
                            failures.append(
                                f"private read-lease pathname no longer identifies opened object: {path}"
                            )
                        else:
                            restored = _path_acl_listing(
                                path, descriptor, accepted_signature, is_directory
                            )
                            if restored != str(record["before"]):
                                failures.append(
                                    f"private read-lease did not restore exact ACL listing: {path}"
                                )
'''
          if text.count(old) != 1:
              raise SystemExit(f"expected one native revoke anchor, found {text.count(old)}")
          text = text.replace(old, new, 1)

          old_constructor = '''        lease = _PrivateReadLease(
            private_subjects,
            lease_repo,
            use_native_darwin_acl=(accepted_root is not None),
        )
'''
          new_constructor = '''        if accepted_root is None:
            lease = _PrivateReadLease(private_subjects, lease_repo)
        else:
            lease = _PrivateReadLease(
                private_subjects,
                lease_repo,
                use_native_darwin_acl=True,
            )
'''
          if text.count(old_constructor) != 1:
              raise SystemExit(
                  f"expected one private-lease constructor anchor, found {text.count(old_constructor)}"
              )
          orchestrator.write_text(text.replace(old_constructor, new_constructor, 1))

          rollback = Path("scripts/ci/tests/test_capture_private_read_lease_rollback.py").read_text()
          required = '''                with self.assertRaisesRegex(
                    helper.SelectedXcodeBuildOrchestratorError,
                    "pathname no longer identifies opened object",
                ):
                    lease.revoke()
                self.assertEqual(subject.read_bytes(), b"attacker replacement\\n")
                self.assertEqual(moved.read_bytes(), b"fixture\\n")
'''
          if rollback.count(required) != 1:
              raise SystemExit(
                  f"expected strict rollback regression already on head, found {rollback.count(required)}"
              )

          composition_path = Path("scripts/ci/tests/test_capture_accepted_root_build_composition.py")
          composition = composition_path.read_text()
          old_fake = '''            def __init__(self, subjects, repo):
                self.subjects = tuple(subjects)
                self.repo = repo
                self._opened: list[object] = []
'''
          new_fake = '''            def __init__(self, subjects, repo, *, use_native_darwin_acl=False):
                self.subjects = tuple(subjects)
                self.repo = repo
                self.use_native_darwin_acl = bool(use_native_darwin_acl)
                self._opened: list[object] = []
'''
          if composition.count(old_fake) != 1:
              raise SystemExit(f"expected one strict FakeLease constructor, found {composition.count(old_fake)}")
          composition = composition.replace(old_fake, new_fake, 1)
          old_assert = '''            self.assertEqual(lease.repo, accepted_root)
            self.assertEqual(
                lease.events,
'''
          new_assert = '''            self.assertEqual(lease.repo, accepted_root)
            self.assertTrue(lease.use_native_darwin_acl)
            self.assertEqual(
                lease.events,
'''
          if composition.count(old_assert) != 1:
              raise SystemExit(f"expected one strict native-mode assertion anchor, found {composition.count(old_assert)}")
          composition_path.write_text(composition.replace(old_assert, new_assert, 1))

          # #3553 temporarily widened a legacy regression fake solely to absorb the
          # unconditional keyword. The production fix preserves the old constructor
          # on non-strict paths, so retire that accidental test-only ABI widening.
          selected_path = Path("scripts/ci/tests/test_capture_selected_xcode_build_orchestrator.py")
          selected = selected_path.read_text()
          widened_fake = '''            def __init__(self, subjects, repo, *, use_native_darwin_acl=False):
                self.subjects = tuple(subjects)
                self.repo = repo
                self.use_native_darwin_acl = bool(use_native_darwin_acl)
                self._opened: list[object] = []
'''
          legacy_fake = '''            def __init__(self, subjects, repo):
                self.subjects = tuple(subjects)
                self.repo = repo
                self._opened: list[object] = []
'''
          if selected.count(widened_fake) != 1:
              raise SystemExit(f"expected one widened legacy FakeLease, found {selected.count(widened_fake)}")
          selected = selected.replace(widened_fake, legacy_fake, 1)
          widened_assert = '''            self.assertEqual(len(FakeLease.instances), 1)
            self.assertFalse(FakeLease.instances[0].use_native_darwin_acl)
            self.assertEqual(
'''
          legacy_assert = '''            self.assertEqual(len(FakeLease.instances), 1)
            self.assertEqual(
'''
          if selected.count(widened_assert) != 1:
              raise SystemExit(f"expected one widened legacy assertion, found {selected.count(widened_assert)}")
          selected_path.write_text(selected.replace(widened_assert, legacy_assert, 1))

      - name: Run focused regressions
        run: |
          set -euo pipefail
          python3 -m py_compile \
            scripts/ci/capture_selected_xcode_build_orchestrator.py \
            scripts/ci/tests/test_capture_private_read_lease_rollback.py \
            scripts/ci/tests/test_capture_accepted_root_build_composition.py \
            scripts/ci/tests/test_capture_selected_xcode_build_orchestrator.py
          python3 scripts/ci/tests/test_capture_private_read_lease_rollback.py
          python3 scripts/ci/tests/test_capture_selected_xcode_build_orchestrator.py
          python3 scripts/ci/tests/test_capture_accepted_root_build_composition.py

      - name: Enforce exact recovery working delta
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git diff --name-only | sort)"
          expected="$(printf '%s\n' \
            scripts/ci/capture_selected_xcode_build_orchestrator.py \
            scripts/ci/tests/test_capture_accepted_root_build_composition.py \
            scripts/ci/tests/test_capture_selected_xcode_build_orchestrator.py | sort)"
          test "$changed" = "$expected"
          ! grep -nE 'publishDps\(|writeValue\(|removeDevice|factoryReset' scripts/ci/capture_selected_xcode_build_orchestrator.py
          git diff --check

      - name: Publish clean recovery product and retire one-shot
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          branch='recovery/v17-capture-native-acl-clean-product-gpt56sol-20260817'
          git fetch origin "$branch"
          test "$(git rev-parse FETCH_HEAD)" = "$GITHUB_SHA"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/ci/capture_selected_xcode_build_orchestrator.py \
                  scripts/ci/tests/test_capture_accepted_root_build_composition.py \
                  scripts/ci/tests/test_capture_selected_xcode_build_orchestrator.py
          git rm .github/workflows/capture-native-acl-failclosed-one-shot.yml
          git diff --cached --check
          git commit -m "Capture: recover clean native ACL fail-closed repair"
          git push origin HEAD:"$branch"
