# V17 exact-head re-arm after the rollback regression landed first.
name: Capture native ACL fail-closed one-shot

on:
  push:
    branches:
      - repair/v17-capture-darwin-native-acl-production-gpt56sol-20260817
    paths:
      - .github/workflows/capture-native-acl-failclosed-one-shot.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Bind exact repair branch
        shell: bash
        run: |
          set -euo pipefail
          test "$GITHUB_REF_NAME" = "repair/v17-capture-darwin-native-acl-production-gpt56sol-20260817"
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test -z "$(git status --porcelain)"

      - name: Materialize fail-closed production repair
        shell: python
        run: |
          from pathlib import Path

          orchestrator = Path("scripts/ci/capture_selected_xcode_build_orchestrator.py")
          text = orchestrator.read_text()
          old = '''                        # Path replacement is not a rollback failure: descriptor custody
                        # intentionally restores the original vnode. Verify path text only
                        # when the canonical name still identifies that exact object.
                        if path_matches:
                            restored = _path_acl_listing(
                                path, descriptor, accepted_signature, is_directory
                            )
                            if restored != str(record["before"]):
                                failures.append(
                                    f"private read-lease did not restore exact ACL listing: {path}"
                                )
'''
          new = '''                        # Descriptor custody restores the original vnode even if the
                        # canonical name was replaced, but namespace identity loss during an
                        # authority-bearing lease is still terminal. Never mutate the replacement.
                        if not path_matches:
                            failures.append(
                                f"private read-lease pathname no longer identifies opened object: {path}"
                            )
                        else:
                            restored = _path_acl_listing(
                                path, descriptor, accepted_signature, is_directory
                            )
                            if restored != str(record["before"]):
                                failures.append(
                                    f"private read-lease did not restore exact ACL listing: {path}"
                                )
'''
          if text.count(old) != 1:
              raise SystemExit(f"expected one native revoke anchor, found {text.count(old)}")
          text = text.replace(old, new, 1)

          # Preserve the legacy constructor shape so the non-strict path and its
          # existing regression doubles do not gain a needless keyword contract.
          old_constructor = '''        lease = _PrivateReadLease(
            private_subjects,
            lease_repo,
            use_native_darwin_acl=(accepted_root is not None),
        )
'''
          new_constructor = '''        if accepted_root is None:
            lease = _PrivateReadLease(private_subjects, lease_repo)
        else:
            lease = _PrivateReadLease(
                private_subjects,
                lease_repo,
                use_native_darwin_acl=True,
            )
'''
          if text.count(old_constructor) != 1:
              raise SystemExit(
                  f"expected one private-lease constructor anchor, found {text.count(old_constructor)}"
              )
          orchestrator.write_text(text.replace(old_constructor, new_constructor, 1))

          rollback = Path("scripts/ci/tests/test_capture_private_read_lease_rollback.py").read_text()
          required = '''                with self.assertRaisesRegex(
                    helper.SelectedXcodeBuildOrchestratorError,
                    "pathname no longer identifies opened object",
                ):
                    lease.revoke()
                self.assertEqual(subject.read_bytes(), b"attacker replacement\\n")
                self.assertEqual(moved.read_bytes(), b"fixture\\n")
'''
          if rollback.count(required) != 1:
              raise SystemExit(
                  f"expected exact strict rollback regression already on head, found {rollback.count(required)}"
              )

      - name: Run focused regressions
        run: |
          python3 scripts/ci/tests/test_capture_private_read_lease_rollback.py
          python3 scripts/ci/tests/test_capture_selected_xcode_build_orchestrator.py

      - name: Enforce exact working delta
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git diff --name-only | sort)"
          test "$changed" = 'scripts/ci/capture_selected_xcode_build_orchestrator.py'
          ! grep -nE 'publishDps\(|writeValue\(|removeDevice|factoryReset' scripts/ci/capture_selected_xcode_build_orchestrator.py
          git diff --check

      - name: Publish clean product repair
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          branch='repair/v17-capture-darwin-native-acl-production-gpt56sol-20260817'
          git fetch origin "$branch"
          test "$(git rev-parse FETCH_HEAD)" = "$GITHUB_SHA"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add scripts/ci/capture_selected_xcode_build_orchestrator.py
          git rm .github/workflows/capture-native-acl-failclosed-one-shot.yml
          git diff --cached --check
          git commit -m "Capture: fail closed on accepted-root pathname replacement"
          git push origin HEAD:"$branch"
