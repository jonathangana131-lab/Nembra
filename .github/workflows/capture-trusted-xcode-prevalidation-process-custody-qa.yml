name: Capture Trusted Xcode Prevalidation Process Custody QA

on:
  pull_request:
    paths:
      - '.github/workflows/capture-xcode27-trusted-command.yml'
      - '.github/workflows/capture-trusted-xcode-prevalidation-process-custody-qa.yml'
      - 'scripts/ci/tests/test_capture_trusted_xcode_prevalidation_process_custody.py'
      - 'scripts/ci/tests/test_capture_trusted_xcode_build_hook_custody.py'

permissions:
  contents: read

jobs:
  prevalidation-process-custody:
    name: Verify trusted prevalidation and build-hook boundaries
    runs-on: ubuntu-latest
    steps:
      - name: Checkout exact PR head
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Verify exact PR head
        env:
          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          actual_head="$(git rev-parse --verify HEAD^{commit})"
          printf 'Expected PR head: %s\nActual checkout head: %s\n' "$EXPECTED_HEAD_SHA" "$actual_head"
          test "$actual_head" = "$EXPECTED_HEAD_SHA"

      - name: Verify process-custody source contracts
        run: |
          python3 -m py_compile scripts/ci/tests/test_capture_trusted_xcode_prevalidation_process_custody.py
          python3 scripts/ci/tests/test_capture_trusted_xcode_prevalidation_process_custody.py
          python3 -m py_compile scripts/ci/tests/test_capture_trusted_xcode_build_hook_custody.py
          python3 scripts/ci/tests/test_capture_trusted_xcode_build_hook_custody.py
