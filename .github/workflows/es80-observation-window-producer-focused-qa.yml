name: ES80 Observation Window Producer Focused QA

on:
  push:
    branches:
      - 'parallel/es80-observation-window-producer/ci/**'

permissions:
  contents: read

concurrency:
  group: nembra-es80-observation-window-producer-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    name: Test observation-window producer package slice
    runs-on: xcode-27
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@v6
      - name: Verify immutable checkout and exact product parent
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$GITHUB_SHA"
          test "$(git rev-parse HEAD^)" = "5d2989fada8506f48f01af1364695fda142142b7"
          test "$(git diff --name-only HEAD^ HEAD)" = ".github/workflows/es80-observation-window-producer-focused-qa.yml"
      - name: Toolchain
        run: |
          set -euo pipefail
          xcodebuild -version
          swift --version
      - name: Guard passive package against application writes
        run: |
          set -euo pipefail
          if grep -R --line-number --include='*.swift' -E '\.writeValue[[:space:]]*\(' Packages/NembraBluetoothCapture/Sources; then
            echo 'Passive ES80 capture package must not call CoreBluetooth writeValue.' >&2
            exit 1
          fi
      - name: Test NembraBluetoothCapture
        run: |
          set -euo pipefail
          swift test --package-path Packages/NembraBluetoothCapture
      - name: Build NembraBluetoothCapture for iOS Simulator
        working-directory: Packages/NembraBluetoothCapture
        run: |
          set -euo pipefail
          xcodebuild -scheme NembraBluetoothCapture -destination 'generic/platform=iOS Simulator' CODE_SIGNING_ALLOWED=NO build
