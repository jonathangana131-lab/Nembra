name: Materialize Capture Foreground Integrity R2

on:
  push:
    branches:
      - "fix/v14-capture-foreground-integrity-r2-sol"

jobs:
  admission:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm branch workflow admission
        run: echo "foreground materializer branch admitted"
