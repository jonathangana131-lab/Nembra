name: Apply Capture Failure Recovery Copy Truth

on:
  push:
    branches:
      - recovery/v14-capture-correlation-progress-truth-gpt56sol
    paths:
      - .github/workflows/apply-capture-failure-recovery-copy-truth.yml

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: recovery/v14-capture-correlation-progress-truth-gpt56sol
          fetch-depth: 0
      - name: Apply and validate failure recovery guidance repair
        run: python3 scripts/ci/apply_capture_failure_recovery_copy_truth.py
