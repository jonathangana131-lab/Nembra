name: Temporary materialize signed app build-origin custody

on:
  push:
    branches:
      - repair/v14-signed-app-build-origin-custody-sol-20260811

permissions:
  contents: write

jobs:
  materialize:
    if: github.event.head_commit.message == 'Run signed app origin patcher'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Placeholder until safe patcher is installed
        run: exit 1
