name: Temporary materialize signed app build-origin custody

on:
  push:
    branches:
      - repair/v14-signed-app-build-origin-custody-sol-20260811

permissions:
  contents: write

jobs:
  materialize:
    if: github.event.head_commit.message == 'Run signed app origin patcher'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      PYTHONPYCACHEPREFIX: /tmp/nembra-origin-pycache
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0

      - name: Bind exact construction head
        shell: bash
        env:
          EXPECTED_HEAD_SHA: ${{ github.sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD_SHA"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

      - name: Normalize one temporary legacy marker escape
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 - <<'PY'
          from pathlib import Path
          path = Path('scripts/ci/tmp_patch_signed_app_origin_existing.py')
          lines = path.read_text(encoding='utf-8').splitlines(keepends=True)
          matches = 0
          repaired = []
          for line in lines:
              if 'build_line=' in line and 'xcodebuild' in line:
                  matches += 1
                  line = line.replace(chr(92) * 4, chr(92) * 2)
              repaired.append(line)
          if matches != 2:
              raise SystemExit(f'expected two temporary build_line literals, found {matches}')
          path.write_text(''.join(repaired), encoding='utf-8')
          PY

      - name: Compile first-class repair and safe patcher
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 -m py_compile \
            scripts/ci/capture_signed_app_build_origin_custody.py \
            scripts/ci/tests/test_capture_signed_app_pre_stage_origin.py \
            scripts/ci/tmp_patch_signed_app_origin_existing.py

      - name: Patch and validate production repair
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 -B scripts/ci/tmp_patch_signed_app_origin_existing.py
          /usr/bin/python3 -m py_compile \
            scripts/ci/capture_signed_app_build_origin_custody.py \
            scripts/ci/capture_signed_app_install_custody.py \
            scripts/ci/tests/test_capture_signed_app_pre_stage_origin.py \
            scripts/ci/tests/test_capture_signed_app_install_custody.py
          /usr/bin/python3 -B -I scripts/ci/tests/test_capture_signed_app_pre_stage_origin.py
          /usr/bin/python3 -B -I scripts/ci/tests/test_capture_signed_app_install_custody.py
          bash -n scripts/field/install_one_time_capture.command
          git diff --check

      - name: Snapshot workflow payloads and publish non-workflow production bytes
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .swarm
          cp .github/workflows/capture-signed-app-install-custody.yml \
            .swarm/materialized_capture_signed_app_install_custody.yml.txt
          cp .github/workflows/capture-field-build-provenance.yml \
            .swarm/materialized_capture_field_build_provenance.yml.txt

          # The Actions token cannot publish workflow-file mutations. Restore those two paths
          # before committing; connected GitHub write authority will apply the exact validated
          # payload snapshots after this non-workflow commit lands.
          git checkout HEAD -- \
            .github/workflows/capture-signed-app-install-custody.yml \
            .github/workflows/capture-field-build-provenance.yml

          rm -f \
            scripts/ci/tmp_materialize_signed_app_build_origin_custody.py \
            scripts/ci/tmp_patch_signed_app_origin_existing.py \
            scripts/ci/tmp_signed_app_origin_installer_block.txt \
            .swarm/tmp_materializer_parser_error.txt \
            .swarm/tmp_origin_patch_error.txt

          git add -A -- \
            scripts/field/install_one_time_capture.command \
            scripts/ci/capture_signed_app_build_origin_custody.py \
            scripts/ci/capture_signed_app_install_custody.py \
            scripts/ci/tests/test_capture_signed_app_pre_stage_origin.py \
            scripts/ci/tests/test_capture_signed_app_install_custody.py \
            scripts/ci/tmp_materialize_signed_app_build_origin_custody.py \
            scripts/ci/tmp_patch_signed_app_origin_existing.py \
            scripts/ci/tmp_signed_app_origin_installer_block.txt \
            .swarm/tmp_materializer_parser_error.txt \
            .swarm/tmp_origin_patch_error.txt \
            .swarm/materialized_capture_signed_app_install_custody.yml.txt \
            .swarm/materialized_capture_field_build_provenance.yml.txt

          git diff --cached --check
          if git diff --cached --name-only | grep '^.github/workflows/' >/dev/null; then
            echo 'ERROR: workflow mutation leaked into Actions-token publication commit.' >&2
            exit 1
          fi
          git config user.name 'nembra-sol-swarm'
          git config user.email 'nembra-sol-swarm@users.noreply.github.com'
          git commit -m 'Bind signed app to build output custody'
          git push origin HEAD:repair/v14-signed-app-build-origin-custody-sol-20260811
