name: V14 refresh terminal go2 current 629

on:
  push:
    branches:
      - integration/es80-v14-current-terminal-fresh-handoff/gpt56-go2

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Merge exact current flagship
        run: |
          set -euo pipefail
          git config user.name nembra-v14-gpt56
          git config user.email actions@users.noreply.github.com
          git merge --no-edit 0ea49c0ab180e34f025a695c1634bc6f7ef6092f
          rm -f .github/workflows/v14-refresh-terminal-go2-current629.yml
          git add -A
          git commit --amend --no-edit
          git diff --check
          git push origin HEAD:integration/es80-v14-current-terminal-fresh-handoff/gpt56-go2
