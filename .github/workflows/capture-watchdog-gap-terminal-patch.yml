name: Capture Watchdog Gap Terminal Patch

on:
  push:
    branches:
      - agent/v14-capture-app-internal-lifecycle-consumption-sol
    paths:
      - .github/workflows/capture-watchdog-gap-terminal-patch.yml

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/v14-capture-app-internal-lifecycle-consumption-sol
          fetch-depth: 0
      - name: Apply correction and self-retire
        shell: bash
        run: |
          set -euo pipefail
          python3 Scripts/apply_capture_watchdog_gap_terminal_patch.py
          rm Scripts/apply_capture_watchdog_gap_terminal_patch.py
          rm .github/workflows/capture-watchdog-gap-terminal-patch.yml
          git config user.name nembra-v14-integration
          git config user.email actions@users.noreply.github.com
          git add -A
          git diff --cached --check
          git diff --cached -- NembraApp/App/NembraCaptureEntrypoint.swift | grep -q 'invalidateObservationContinuity'
          git commit -m "fix(capture): preserve watchdog gap terminal ownership"
          git push origin HEAD:agent/v14-capture-app-internal-lifecycle-consumption-sol
