name: Validate authenticated transparent live wiring

on:
  push:
    branches:
      - capture/one-time-ble-dump-gpt56
    paths:
      - 'Packages/NembraBluetoothCapture/**'
      - 'Packages/NembraCore/**'
      - 'Nembra/**'
      - 'NembraApp/**'
      - 'NembraCapture/**'
      - 'NembraCaptureTests/**'
      - 'NembraAppTests/**'
      - 'Nembra.xcodeproj/**'
      - '.github/workflows/validate-authenticated-transparent-live-wiring.yml'
  pull_request:
    paths:
      - 'Packages/NembraBluetoothCapture/**'
      - 'Packages/NembraCore/**'
      - 'Nembra/**'
      - 'NembraApp/**'
      - 'NembraCapture/**'
      - 'NembraCaptureTests/**'
      - 'NembraAppTests/**'
      - 'Nembra.xcodeproj/**'
      - '.github/workflows/validate-authenticated-transparent-live-wiring.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: authenticated-transparent-live-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  validate-live-wiring:
    name: Package tests + physical iOS compile
    runs-on: xcode-27
    timeout-minutes: 25

    steps:
      - name: Resolve immutable source head
        id: source
        shell: bash
        env:
          PR_HEAD_SHA: ${{ github.event.pull_request.head.sha }}
        run: |
          set -euo pipefail
          if [[ "${GITHUB_EVENT_NAME}" == "pull_request" ]]; then
            test -n "$PR_HEAD_SHA"
            printf 'sha=%s\n' "$PR_HEAD_SHA" >> "$GITHUB_OUTPUT"
          else
            printf 'sha=%s\n' "$GITHUB_SHA" >> "$GITHUB_OUTPUT"
          fi

      - name: Checkout immutable exact head
        uses: actions/checkout@v6
        with:
          ref: ${{ steps.source.outputs.sha }}
          fetch-depth: 1
          persist-credentials: false

      - name: Verify exact checkout and trusted toolchain
        shell: bash
        env:
          EXPECTED_HEAD_SHA: ${{ steps.source.outputs.sha }}
        run: |
          set -euo pipefail
          actual="$(git rev-parse --verify HEAD^{commit})"
          printf 'Expected exact head: %s\nActual checkout: %s\n' "$EXPECTED_HEAD_SHA" "$actual"
          test "$actual" = "$EXPECTED_HEAD_SHA"
          xcodebuild -version
          swift --version
          plutil -lint Nembra.xcodeproj/project.pbxproj
          scripts/validate_pbxproj_references.py

      - name: Run Bluetooth capture package tests
        id: package_tests
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          swift test --package-path Packages/NembraBluetoothCapture 2>&1 | tee "${RUNNER_TEMP}/nembra-bluetooth-capture-tests.log"

      - name: Compile authenticated live wiring for physical iOS
        id: physical_ios_compile
        continue-on-error: true
        shell: bash
        run: |
          set -o pipefail
          derived="${RUNNER_TEMP}/NembraAuthenticatedTransparentLive"
          rm -rf "$derived"
          xcodebuild \
            -project Nembra.xcodeproj \
            -scheme Nembra \
            -configuration Debug \
            -destination 'generic/platform=iOS' \
            -derivedDataPath "$derived" \
            CODE_SIGNING_ALLOWED=NO \
            build 2>&1 | tee "${RUNNER_TEMP}/nembra-authenticated-transparent-live-build.log"

      - name: Record semantics-free validation boundary
        if: always()
        shell: bash
        env:
          PACKAGE_TEST_OUTCOME: ${{ steps.package_tests.outcome }}
          PHYSICAL_IOS_COMPILE_OUTCOME: ${{ steps.physical_ios_compile.outcome }}
        run: |
          set -euo pipefail
          derived="${RUNNER_TEMP}/NembraAuthenticatedTransparentLive"
          app_bundle="$(find "$derived/Build/Products" -maxdepth 3 -type d -name '*.app' -print -quit 2>/dev/null || true)"
          app_bundle_present=false
          if [[ -n "$app_bundle" && -f "$app_bundle/Info.plist" ]]; then
            app_bundle_present=true
          fi

          printf 'AUTHENTICATED_TRANSPARENT_LIVE_VALIDATION head=%s packageTests=%s physicalIOSCompile=%s appBundlePresent=%s physicalHardwareEvidence=false dpSemanticsAssigned=false scooterWritesAuthorized=false resetOrUnbindAuthorized=false\n' \
            "${{ steps.source.outputs.sha }}" "$PACKAGE_TEST_OUTCOME" "$PHYSICAL_IOS_COMPILE_OUTCOME" "$app_bundle_present" | tee -a "$GITHUB_STEP_SUMMARY"

          if [[ "$PACKAGE_TEST_OUTCOME" != "success" || "$PHYSICAL_IOS_COMPILE_OUTCOME" != "success" ]]; then
            printf 'Validation failed: packageTests=%s physicalIOSCompile=%s\n' "$PACKAGE_TEST_OUTCOME" "$PHYSICAL_IOS_COMPILE_OUTCOME" >&2
            exit 1
          fi

          if [[ "$app_bundle_present" != "true" ]]; then
            printf 'Warning: xcodebuild succeeded but no .app bundle was found under the configured DerivedData products directory. Treat compile outcome as authoritative and keep physicalHardwareEvidence=false.\n' >&2
          fi
