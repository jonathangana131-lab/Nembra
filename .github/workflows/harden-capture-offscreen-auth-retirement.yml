name: Harden Capture Offscreen Auth Retirement

on:
  push:
    branches:
      - agent/capture-offscreen-auth-current-fd6f

jobs:
  harden:
    runs-on: ubuntu-latest
    steps:
      - run: echo workflow-started
