name: TMP V14 Field Tracked Source Window Sol

on:
  push:
    branches:
      - repair/v14-field-tracked-source-window-sol-20260811
    paths:
      - scripts/ci/tmp_materialize_field_tracked_source_window_sol.py
      - .github/workflows/tmp-v14-field-tracked-source-window-sol.yml

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.sha }}
          fetch-depth: 0
      - name: Bind exact construction head
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${GITHUB_SHA}"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"
      - name: Materialize and validate tracked-source custody
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 -I scripts/ci/tmp_materialize_field_tracked_source_window_sol.py
