name: Capture Field Selected Xcode Tool File Custody Red Team

on:
  push:
    branches:
      - adversarial/v14-selected-xcodebuild-file-custody-r2-sol-4c47a9-20260811
    paths:
      - scripts/field/install_one_time_capture.command
      - scripts/ci/tests/test_capture_field_selected_xcode_tool_file_custody_red_team.py
      - .github/workflows/capture-field-selected-xcode-tool-file-custody-red-team.yml
  pull_request:
    paths:
      - scripts/field/install_one_time_capture.command
      - scripts/ci/tests/test_capture_field_selected_xcode_tool_file_custody_red_team.py
      - .github/workflows/capture-field-selected-xcode-tool-file-custody-red-team.yml
  workflow_dispatch:

permissions:
  contents: read

jobs:
  selected-xcode-tool-file-custody:
    name: Require exact selected Xcode executable authority
    if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}

      - name: Bind diagnostic to exact checked-out head
        shell: bash
        env:
          EXPECTED_HEAD_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_HEAD_SHA"
          test -z "$(git status --porcelain=v1 --untracked-files=all)"

      - name: Compile diagnostic
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 -m py_compile scripts/ci/tests/test_capture_field_selected_xcode_tool_file_custody_red_team.py

      - name: Require exact selected Xcode executable custody
        shell: bash
        run: |
          set -euo pipefail
          /usr/bin/python3 -I scripts/ci/tests/test_capture_field_selected_xcode_tool_file_custody_red_team.py
