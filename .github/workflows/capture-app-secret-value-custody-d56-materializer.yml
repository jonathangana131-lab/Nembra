name: Capture app-secret value custody materializer

on:
  push:
    branches:
      - fix/v14-capture-app-secret-value-custody-d56-sol

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Materialize exact-current secret custody repair
        run: python3 .github/materializers/capture_app_secret_value_custody_d56.py
