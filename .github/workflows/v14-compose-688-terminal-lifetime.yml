name: V14 compose 688 terminal lifetime

on:
  push:
    branches:
      - integration/es80-v14-current-terminal-fresh-handoff/gpt56-go2

permissions:
  contents: write

jobs:
  compose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Merge exact recorder lifetime hardening
        run: |
          set -euo pipefail
          git config user.name nembra-v14-gpt56
          git config user.email actions@users.noreply.github.com
          git merge --no-edit 6a9f06e1c43fa0039298d1480a45f447d70f3b47
          rm -f .github/workflows/v14-compose-688-terminal-lifetime.yml
          git add -A
          git commit --amend --no-edit
          git diff --check
          git push origin HEAD:integration/es80-v14-current-terminal-fresh-handoff/gpt56-go2
