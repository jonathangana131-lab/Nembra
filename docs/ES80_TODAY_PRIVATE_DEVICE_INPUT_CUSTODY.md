# ES80 TODAY Private Intended-Device Input Custody — RETIRED / NON-AUTHORIZING

Status: **RETIRED HISTORICAL HANDOFF. PHYSICAL STATUS: NO-GO.**

This document belonged to the superseded private TODAY / `ES80-FINGERPRINT-v1` signing lane and was pinned to its historical frozen product subject and helper blobs. Those pins and operator instructions are no longer current build/sign/install authority and must not be reconstructed into the authenticated-stationary field path.

The current physical procedure is `docs/CAPTURE_P0_SECURE_LINK_NEXT_TEST.md` with procedure identifier **`ES80-AUTHENTICATED-STATIONARY-v1`**. Any private intended-device identifier used by a current signed candidate must be acquired, stored, and validated through the current reviewed private-input/provisioning authority named by the live lineage. Do not copy an old helper SHA or frozen source pin merely because the historical custody design was hardened.

The enduring security boundary remains: private device identifiers, AppKey/AppSecret, account tokens, verification codes, local keys, and generated security material must stay out of Git, logs, screenshots, public artifacts, and process arguments where the current contract forbids them. That boundary is not physical authorization.

Historical helper implementations and adversarial tests may remain in the repository for regression evidence. Their presence does not make an old TODAY handoff operational. Until the current authenticated-stationary control plane explicitly earns Final GO for one exact accepted signed install, **PHYSICAL STATUS: NO-GO / DO NOT SCAN / DO NOT RUN**.
