# ES80 TODAY Research Authorization Contract — RETIRED / NON-AUTHORIZING

Status: **RETIRED. PHYSICAL STATUS: NO-GO. THIS HISTORICAL PRIVATE `ES80-FINGERPRINT-v1` AUTHORIZATION CONTRACT MUST NOT AUTHORIZE THE CURRENT CAPTURE PRODUCT.**

This contract defined the superseded TODAY Research-build exception, including historical `ResearchAdmission` / build-time authority for the first passive fingerprint artifact. That lane has been retired. Its historical package capability, recipe binding, build markers, and private-production exception must not be used to bypass or downgrade the current authenticated-stationary control plane.

The current physical procedure is `docs/CAPTURE_P0_SECURE_LINK_NEXT_TEST.md` and its exact procedure identifier is **`ES80-AUTHENTICATED-STATIONARY-v1`**. Current field-build authority, Tuya account/device membership authority, signed intended-device install authority, exact-head acceptance, and Final-GO evidence must be resolved from the live current lineage.

Historical ResearchAdmission implementation and regression fixtures may remain in the repository as evidence that old authority cannot leak into ordinary builds. Their presence is not current authorization. A historical build marker, recipe, signed IPA, exact-head green run, or recovered older commit cannot mint current physical GO.

Until the current authenticated-stationary procedure explicitly earns a valid Final-GO result for one exact accepted candidate, **PHYSICAL STATUS: NO-GO / DO NOT SCAN / DO NOT RUN**.
