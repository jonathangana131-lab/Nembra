# AOVOPRO ES80 Passive Physical Capture Runbook — retired V14 lane

RETIRED V14 LANE

Status: **RETIRED / NON-AUTHORITATIVE / PHYSICAL NO-GO.**

This file is a tombstone for the former passive Experiment One path. It is **not** the current physical-procedure gate and must not be edited from NO-GO to GO for the current Capture product.

Current procedure authority lives in `docs/CAPTURE_P0_SECURE_LINK_NEXT_TEST.md` under procedure `ES80-AUTHENTICATED-STATIONARY-v1`.

The completed historical capture remains useful evidence, but it cannot authorize a new run. Queued, running, skipped, ancestor-green, package-only, Simulator-only, source-review-only, self-described, or historical passive evidence cannot authorize that experiment.

Do not revive this retired lane to bypass the current exact-build, same-account device authority, four-window target correlation, supported SmartLife SDK authentication, authenticated-observation, export-integrity, visual/runtime, signing/install, intended-device rendezvous, or final repository GO gates.

Historical CoreBluetooth UUID/name/RSSI/FD50/Tuya hints remain descriptive evidence only. They are never fallback target authority. Simulator evidence remains software evidence. Public evidence is not physical verification.

The current physical status is **NO-GO**. Follow only the current canonical runbook after the repository explicitly records GO on the final composed exact build.
