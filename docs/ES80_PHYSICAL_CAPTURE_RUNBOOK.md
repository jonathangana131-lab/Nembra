# AOVOPRO ES80 Passive Physical Capture Runbook — RETIRED V14 LANE

Status: **RETIRED / NON-AUTHORITATIVE / PHYSICAL NO-GO.**

Primary physical target: the current/newer Tuya-generation AOVOPRO ES80.

This file is retained as an audit marker for the earlier passive Experiment One lineage. It is **not** the current physical-procedure gate and it must not be edited from NO-GO to GO for the current Capture product.

## Current procedure authority

Current shipping source mechanically requires:

`ES80-AUTHENTICATED-STATIONARY-v1`

The current operator procedure and GO/NO-GO contract is:

`docs/CAPTURE_P0_SECURE_LINK_NEXT_TEST.md`

That runbook is the continuation after physical capture `C7D09A22` and owns the next smallest stationary, authenticated, read-only Tuya experiment. It requires fresh package-owned OFF1 -> ON1 -> OFF2 -> ON2 correlation, explicit target confirmation, official Tuya SDK account authority, exact same-account scooter membership/UID lease, Tuya-owned authenticated BLE, canonical current-generation observation chronology, and exact final build/device/provenance acceptance.

If this historical file conflicts with shipping source, the current secure-link runbook, or a later accepted exact-head procedure contract, **this historical file loses**. Any mismatch is NO-GO.

## Retired passive procedure — historical only

The former procedure in this file used the passive `ES80-FINGERPRINT-v1` lineage, including passive GATT acquisition, a 60-second post-Ready/Horizon model, release-grade P-256 / external field-authorization envelope concepts, and a `PassiveBluetoothExperimentOneFieldExecutionGate` final record.

Those mechanisms remain useful historical design/evidence context, but they do not authorize the current authenticated stationary product and cannot be used to bypass the current Tuya SDK account/device, signed-build, runtime, provenance, visual, lifecycle, or exact-head gates.

The full retired text remains recoverable from Git history. It is intentionally not kept here as runnable operator instructions.

## Historical physical evidence boundary

Physical capture `C7D09A22-96DA-4E46-9BEF-E36F670ADB0E` remains immutable historical evidence. It established transport-family observations including Tuya FD50 and repeated approximately 30-second peripheral-initiated disconnects, while producing zero application characteristic payloads.

That historical capture does **not** establish battery, speed, voltage, current, power, mode, trip, odometer, DP meanings, command acknowledgement, permanent scooter identity, or safe write authority. Do not repeat the old ride merely because the current authenticated stationary gate is blocked.

## Current physical boundary

**NO-GO / DO NOT SCAN / DO NOT RUN / DO NOT REPEAT THE OLD RIDE** until `docs/CAPTURE_P0_SECURE_LINK_NEXT_TEST.md` and the final composed product explicitly authorize one exact build/install for `ES80-AUTHENTICATED-STATIONARY-v1`.

Queued, running, skipped, ancestor-green, package-only, Simulator-only, source-review-only, self-described, or historical passive evidence cannot authorize that experiment.

## Safety and truth

Retiring this document creates no Bluetooth write authority, DP query/publish authority, scooter command authority, protocol semantics, telemetry semantics, physical identity, or physical acceptance. The current P0 experiment remains stationary/read-only; Nembra must not guess packets, DP meanings, or commands to compensate for missing authenticated evidence.

## Control-plane invariant

The final physical GO path must keep the following coherent on one accepted product lineage:

1. the procedure identifier required by shipping source;
2. the operator runbook used in the field;
3. the exact signed/installable build and runtime provenance; and
4. the final durable GO record.

If any subject disagrees, the answer is **NO-GO**.
