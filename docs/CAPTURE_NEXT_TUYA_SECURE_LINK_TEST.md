# DEPRECATED / DO NOT USE — legacy Tuya secure-link pointer

PHYSICAL STATUS: NO-GO

This tombstone contains no executable field instructions. Use only `docs/CAPTURE_P0_SECURE_LINK_NEXT_TEST.md` and `scripts/field/install_one_time_capture.command` for the current `ES80-AUTHENTICATED-STATIONARY-v1` handoff.

Current target correlation is the complete fresh `OFF1 → ON1 → OFF2 → ON2` procedure with exactly one repeatable package result, explicit current-attempt confirmation, and no historical UUID/name/RSSI/FD50/Tuya-hint fallback authority.

This pointer cannot authorize scanning, a scooter, an account, an install, Bluetooth ownership, or physical Capture.
