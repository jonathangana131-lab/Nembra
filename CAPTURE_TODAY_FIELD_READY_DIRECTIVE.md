# CAPTURE TODAY FIELD-READY DIRECTIVE — V14

Status: **SUPERSEDED / NO FIELD AUTHORITY / PHYSICAL NO-GO.**

Feature: **Nembra Capture / authenticated stationary ES80 evidence**

This file is retained only as a tombstone for the historical private Research path that targeted `ES80-FINGERPRINT-v1`.

The old TODAY override is retired. It must not authorize a scan, signed candidate, install, runtime rendezvous, experiment, or physical claim. The current Capture product requires the canonical procedure:

`ES80-AUTHENTICATED-STATIONARY-v1`

Current executable field authority is intentionally narrow and must agree end-to-end:

- `scripts/field/install_one_time_capture.command` builds and installs the authenticated standalone Capture product from an exact software-accepted source SHA.
- `NembraApp/App/NembraCaptureBuildIdentity.swift` requires the same canonical procedure in the running app provenance.
- `.github/workflows/capture-field-build-provenance.yml` checks the installer, built app, private Tuya dependency provenance, intended iPhone 12 / iOS 27 admission, and procedure rendezvous.

The historical `NEMBRA_ES80_TODAY_RESEARCH` / `ES80-FINGERPRINT-v1` producer path is not a fallback and must not be used to bypass current authenticated procedure gates. Historical artifacts produced under that older path remain historical evidence only; this retirement does not rewrite or promote them.

A future physical GO requires one final composed exact Capture product head to earn all required software, provenance, visual/accessibility, signing, intended-device, official-account/membership/UID-lease, and runtime rendezvous gates for `ES80-AUTHENTICATED-STATIONARY-v1`.

Until that exact current chain is complete:

**PHYSICAL STATUS: NO-GO / DO NOT SCAN / DO NOT RUN / DO NOT REPEAT THE OLD RIDE.**
