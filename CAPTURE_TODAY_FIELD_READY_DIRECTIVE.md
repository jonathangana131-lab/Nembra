# RETIRED / DO NOT USE — Capture TODAY directive

Status: **RETIRED / NON-AUTHORITATIVE / PHYSICAL NO-GO**

This file previously described the superseded `ES80-FINGERPRINT-v1` research-build path and a 60-second acceptance rule. It is retained only so old links fail closed and explain where current authority lives.

The only current Capture procedure is:

- `ES80-AUTHENTICATED-STATIONARY-v1`
- canonical runbook: `docs/CAPTURE_P0_SECURE_LINK_NEXT_TEST.md`
- canonical installer: `scripts/field/install_one_time_capture.command`
- canonical authenticated observation minimum: 45 seconds

Those historical mechanisms **must not** be used to bypass, downgrade, or substitute for the current authenticated stationary gates. In particular, the retired compile flag, old signed-candidate producer, historical physical UUID, and this document cannot authorize Bluetooth, a scooter, an account, an install, or a physical attempt.

The current path remains physical **NO-GO** until the exact accepted source and dependency lock are privately built, signed, verified, installed on the intended iPhone 12/iOS 27 device, and the app freshly proves its account, exact scooter membership, current correlation, operator-declared stationary/charger-disconnected/no-riding safety conditions, and read-only session gates.

No outdoor ride is authorized. Nembra sends no scooter DP query/control command and opens no second post-authentication CoreBluetooth connection. A public CI or Simulator pass is software evidence only.
