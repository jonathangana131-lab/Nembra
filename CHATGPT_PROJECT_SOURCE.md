# Nembra — ChatGPT Project Source

Repository: `jonathangana131-lab/Nembra`

GitHub is the source of truth. On every coding run, read the latest root `AGENTS.md` from `main` and follow it. Current `main`, live PRs/reviews, current product/safety docs, and exact evidence outrank stale chat memory, old swarm state, old branch checklists, and old hosted-CI wording.

When the user says `Go`, `continue`, `keep going`, `work on Nembra`, `finish Nembra`, `finish Nembra 1.0`, or equivalent, immediately begin real GitHub work and continue for the whole available turn while useful work remains. Do not answer with only a plan, status report, task list, or question about what to work on when live GitHub can decide.

Use the connected GitHub app to refresh `main`, open PRs, recent merges, reviews/checks, and relevant current product/safety/release docs. Prefer finishing or converging strong existing work over creating a competing branch. Implement/review/integrate real outcomes, verify/fix `main`, refresh, and continue.

## Do not depend on one execution environment

Nembra development must continue normally if Codex usage is exhausted, one chat has no shell/Xcode, GitHub Actions is unavailable/queued/out of minutes, or one Mac/runner is offline.

- Capable environments run Swift/Xcode/Simulator/tests directly.
- Hosted Actions/Xcode is useful supplemental evidence, not privileged ordinary-development authority.
- Connector-only chats keep doing real source work and may use the root `AGENTS.md` development-main fast path for bounded source-complete ordinary changes, recording execution honestly as pending.
- Missing execution is never permission to fabricate PASS, weaken tests, or widen physical authority.
- Release, physical BLE/Tuya truth, signing/key custody, real-device evidence, final visual/accessibility/performance acceptance, and known fail-first/source-incomplete work remain strict.

Development `main` is an integration trunk, not automatic release acceptance.

## Product target

Keep moving until the bounded **Nembra 1.0** product is actually release-complete, not merely until a large integration branch exists. `docs/AUTONOMY_STATUS.md` provides machine-readable milestone state and must be updated when truth changes.

- `NEMBRA_1_0_RELEASED` stays false until exact release source satisfies applicable acceptance and the intended 1.0 release/tag/publication exists.
- `CAPTURE_USER_INPUT_READY` stays false until Nembra Capture's software-side read-only stationary-capture prerequisites are accepted and the next real blocker is specifically the user's fresh iPhone/scooter/account Bluetooth session.

When Capture becomes ready for that user-provided physical input, future agents must say so clearly and provide the exact safe current procedure. Do not ask early just because a draft says “next physical rung.”

## Physical truth

Never invent physical scooter evidence, BLE/Tuya semantics, telemetry mappings, command behavior, firmware behavior, or authorization. Software/Simulator evidence is not physical-device proof. Do not send scooter commands unless current product/safety docs and fresh evidence explicitly authorize the exact operation. Keep private keys, credentials, identifiers, raw sensitive capture data, and private signed artifacts out of GitHub.

If a physical lane is blocked, preserve the exact blocker and continue independent software/product development instead of stalling or manufacturing evidence.

## Coordination

Do not revive the historical Nembra swarm scheduler, worker quotas, mission graph, custom claims, leases, heartbeats, fencing, capacity mining, or recovery-branch ladders. Ordinary GitHub branches/PRs are the coordination layer. No fixed writer count is required; parallelism follows genuinely independent work and integration pressure.

For all other product, quality, testing, merge, Capture, and release rules, defer to the latest root `AGENTS.md` and current repository evidence.
